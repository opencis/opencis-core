"""
Auto-generated pure-Python fallback for the Cython packet_structs extension.
Generated by generate_py_fallback.py — do NOT edit by hand.

This module provides identical API to the compiled Cython extension so the
codebase runs without Microsoft Visual C++ Build Tools.
"""

# ---------------------------------------------------------------------------
# Bit manipulation helpers (mirrors the Cython _read_bits / _write_bits)
# ---------------------------------------------------------------------------

def _read_bits(buf: bytearray, start_bit: int, width: int) -> int:
    result = 0
    for i in range(width):
        byte_index = (start_bit + i) >> 3
        bit_offset = (start_bit + i) & 7
        if (buf[byte_index] >> bit_offset) & 1:
            result |= 1 << i
    return result


def _write_bits(buf: bytearray, start_bit: int, width: int, value: int) -> None:
    for i in range(width):
        byte_index = (start_bit + i) >> 3
        bit_offset = (start_bit + i) & 7
        if (value >> i) & 1:
            buf[byte_index] |= 1 << bit_offset
        else:
            buf[byte_index] &= ~(1 << bit_offset)

# ---------------------------------------------------------------------------
# Header structs (one class per fields.py entry)
# ---------------------------------------------------------------------------

class SystemHeader:
    _size = 2

    def __init__(self):
        self._buf = bytearray(2)
        self._p = 0  # byte-offset into parent buffer (set by attach)
        self._parent_buf = self._buf  # default: own buffer

    def _attach(self, parent_buf: bytearray, offset: int):
        self._parent_buf = parent_buf
        self._p = offset

    # field: payload_type  bits 0-3 (4 b)
    @property
    def payload_type(self):
        return _read_bits(self._parent_buf, self._p * 8 + 0, 4)
    @payload_type.setter
    def payload_type(self, v):
        _write_bits(self._parent_buf, self._p * 8 + 0, 4, v)

    # field: payload_length  bits 4-15 (12 b)
    @property
    def payload_length(self):
        return _read_bits(self._parent_buf, self._p * 8 + 4, 12)
    @payload_length.setter
    def payload_length(self, v):
        _write_bits(self._parent_buf, self._p * 8 + 4, 12, v)

    @classmethod
    def get_size(cls):
        return 2

    def __len__(self):
        return 2

    def __bytes__(self):
        return bytes(self._parent_buf[self._p : self._p + 2])

class SidebandHeader:
    _size = 1

    def __init__(self):
        self._buf = bytearray(1)
        self._p = 0  # byte-offset into parent buffer (set by attach)
        self._parent_buf = self._buf  # default: own buffer

    def _attach(self, parent_buf: bytearray, offset: int):
        self._parent_buf = parent_buf
        self._p = offset

    # field: type  bits 0-7 (8 b)
    @property
    def type(self):
        return _read_bits(self._parent_buf, self._p * 8 + 0, 8)
    @type.setter
    def type(self, v):
        _write_bits(self._parent_buf, self._p * 8 + 0, 8, v)

    @classmethod
    def get_size(cls):
        return 1

    def __len__(self):
        return 1

    def __bytes__(self):
        return bytes(self._parent_buf[self._p : self._p + 1])

class TlpPrefix:
    _size = 4

    def __init__(self):
        self._buf = bytearray(4)
        self._p = 0  # byte-offset into parent buffer (set by attach)
        self._parent_buf = self._buf  # default: own buffer

    def _attach(self, parent_buf: bytearray, offset: int):
        self._parent_buf = parent_buf
        self._p = offset

    # field: pcie_base_spec_defined  bits 0-7 (8 b)
    @property
    def pcie_base_spec_defined(self):
        return _read_bits(self._parent_buf, self._p * 8 + 0, 8)
    @pcie_base_spec_defined.setter
    def pcie_base_spec_defined(self, v):
        _write_bits(self._parent_buf, self._p * 8 + 0, 8, v)

    # field: ld_id  bits 8-23 (16 b)
    @property
    def ld_id(self):
        return _read_bits(self._parent_buf, self._p * 8 + 8, 16)
    @ld_id.setter
    def ld_id(self, v):
        _write_bits(self._parent_buf, self._p * 8 + 8, 16, v)

    # field: reserved  bits 24-31 (8 b)
    @property
    def reserved(self):
        return _read_bits(self._parent_buf, self._p * 8 + 24, 8)
    @reserved.setter
    def reserved(self, v):
        _write_bits(self._parent_buf, self._p * 8 + 24, 8, v)

    @classmethod
    def get_size(cls):
        return 4

    def __len__(self):
        return 4

    def __bytes__(self):
        return bytes(self._parent_buf[self._p : self._p + 4])

class CxlIoHeader:
    _size = 4

    def __init__(self):
        self._buf = bytearray(4)
        self._p = 0  # byte-offset into parent buffer (set by attach)
        self._parent_buf = self._buf  # default: own buffer

    def _attach(self, parent_buf: bytearray, offset: int):
        self._parent_buf = parent_buf
        self._p = offset

    # field: fmt_type  bits 0-7 (8 b)
    @property
    def fmt_type(self):
        return _read_bits(self._parent_buf, self._p * 8 + 0, 8)
    @fmt_type.setter
    def fmt_type(self, v):
        _write_bits(self._parent_buf, self._p * 8 + 0, 8, v)

    # field: th  bits 8-8 (1 b)
    @property
    def th(self):
        return _read_bits(self._parent_buf, self._p * 8 + 8, 1)
    @th.setter
    def th(self, v):
        _write_bits(self._parent_buf, self._p * 8 + 8, 1, v)

    # field: rsvd  bits 9-9 (1 b)
    @property
    def rsvd(self):
        return _read_bits(self._parent_buf, self._p * 8 + 9, 1)
    @rsvd.setter
    def rsvd(self, v):
        _write_bits(self._parent_buf, self._p * 8 + 9, 1, v)

    # field: attr_b2  bits 10-10 (1 b)
    @property
    def attr_b2(self):
        return _read_bits(self._parent_buf, self._p * 8 + 10, 1)
    @attr_b2.setter
    def attr_b2(self, v):
        _write_bits(self._parent_buf, self._p * 8 + 10, 1, v)

    # field: t8  bits 11-11 (1 b)
    @property
    def t8(self):
        return _read_bits(self._parent_buf, self._p * 8 + 11, 1)
    @t8.setter
    def t8(self, v):
        _write_bits(self._parent_buf, self._p * 8 + 11, 1, v)

    # field: tc  bits 12-14 (3 b)
    @property
    def tc(self):
        return _read_bits(self._parent_buf, self._p * 8 + 12, 3)
    @tc.setter
    def tc(self, v):
        _write_bits(self._parent_buf, self._p * 8 + 12, 3, v)

    # field: t9  bits 15-15 (1 b)
    @property
    def t9(self):
        return _read_bits(self._parent_buf, self._p * 8 + 15, 1)
    @t9.setter
    def t9(self, v):
        _write_bits(self._parent_buf, self._p * 8 + 15, 1, v)

    # field: length_upper  bits 16-17 (2 b)
    @property
    def length_upper(self):
        return _read_bits(self._parent_buf, self._p * 8 + 16, 2)
    @length_upper.setter
    def length_upper(self, v):
        _write_bits(self._parent_buf, self._p * 8 + 16, 2, v)

    # field: at  bits 18-19 (2 b)
    @property
    def at(self):
        return _read_bits(self._parent_buf, self._p * 8 + 18, 2)
    @at.setter
    def at(self, v):
        _write_bits(self._parent_buf, self._p * 8 + 18, 2, v)

    # field: attr  bits 20-21 (2 b)
    @property
    def attr(self):
        return _read_bits(self._parent_buf, self._p * 8 + 20, 2)
    @attr.setter
    def attr(self, v):
        _write_bits(self._parent_buf, self._p * 8 + 20, 2, v)

    # field: ep  bits 22-22 (1 b)
    @property
    def ep(self):
        return _read_bits(self._parent_buf, self._p * 8 + 22, 1)
    @ep.setter
    def ep(self, v):
        _write_bits(self._parent_buf, self._p * 8 + 22, 1, v)

    # field: td  bits 23-23 (1 b)
    @property
    def td(self):
        return _read_bits(self._parent_buf, self._p * 8 + 23, 1)
    @td.setter
    def td(self, v):
        _write_bits(self._parent_buf, self._p * 8 + 23, 1, v)

    # field: length_lower  bits 24-31 (8 b)
    @property
    def length_lower(self):
        return _read_bits(self._parent_buf, self._p * 8 + 24, 8)
    @length_lower.setter
    def length_lower(self, v):
        _write_bits(self._parent_buf, self._p * 8 + 24, 8, v)

    @classmethod
    def get_size(cls):
        return 4

    def __len__(self):
        return 4

    def __bytes__(self):
        return bytes(self._parent_buf[self._p : self._p + 4])

class CxlIoMReqHeader:
    _size = 12

    def __init__(self):
        self._buf = bytearray(12)
        self._p = 0  # byte-offset into parent buffer (set by attach)
        self._parent_buf = self._buf  # default: own buffer

    def _attach(self, parent_buf: bytearray, offset: int):
        self._parent_buf = parent_buf
        self._p = offset

    # field: req_id  bits 0-15 (16 b)
    @property
    def req_id(self):
        return _read_bits(self._parent_buf, self._p * 8 + 0, 16)
    @req_id.setter
    def req_id(self, v):
        _write_bits(self._parent_buf, self._p * 8 + 0, 16, v)

    # field: tag  bits 16-23 (8 b)
    @property
    def tag(self):
        return _read_bits(self._parent_buf, self._p * 8 + 16, 8)
    @tag.setter
    def tag(self, v):
        _write_bits(self._parent_buf, self._p * 8 + 16, 8, v)

    # field: first_dw_be  bits 24-27 (4 b)
    @property
    def first_dw_be(self):
        return _read_bits(self._parent_buf, self._p * 8 + 24, 4)
    @first_dw_be.setter
    def first_dw_be(self, v):
        _write_bits(self._parent_buf, self._p * 8 + 24, 4, v)

    # field: last_dw_be  bits 28-31 (4 b)
    @property
    def last_dw_be(self):
        return _read_bits(self._parent_buf, self._p * 8 + 28, 4)
    @last_dw_be.setter
    def last_dw_be(self, v):
        _write_bits(self._parent_buf, self._p * 8 + 28, 4, v)

    # field: addr_upper  bits 32-87 (56 b)
    @property
    def addr_upper(self):
        return _read_bits(self._parent_buf, self._p * 8 + 32, 56)
    @addr_upper.setter
    def addr_upper(self, v):
        _write_bits(self._parent_buf, self._p * 8 + 32, 56, v)

    # field: rsvd  bits 88-89 (2 b)
    @property
    def rsvd(self):
        return _read_bits(self._parent_buf, self._p * 8 + 88, 2)
    @rsvd.setter
    def rsvd(self, v):
        _write_bits(self._parent_buf, self._p * 8 + 88, 2, v)

    # field: addr_lower  bits 90-95 (6 b)
    @property
    def addr_lower(self):
        return _read_bits(self._parent_buf, self._p * 8 + 90, 6)
    @addr_lower.setter
    def addr_lower(self, v):
        _write_bits(self._parent_buf, self._p * 8 + 90, 6, v)

    @classmethod
    def get_size(cls):
        return 12

    def __len__(self):
        return 12

    def __bytes__(self):
        return bytes(self._parent_buf[self._p : self._p + 12])

class CxlIoCfgReqHeader:
    _size = 8

    def __init__(self):
        self._buf = bytearray(8)
        self._p = 0  # byte-offset into parent buffer (set by attach)
        self._parent_buf = self._buf  # default: own buffer

    def _attach(self, parent_buf: bytearray, offset: int):
        self._parent_buf = parent_buf
        self._p = offset

    # field: req_id  bits 0-15 (16 b)
    @property
    def req_id(self):
        return _read_bits(self._parent_buf, self._p * 8 + 0, 16)
    @req_id.setter
    def req_id(self, v):
        _write_bits(self._parent_buf, self._p * 8 + 0, 16, v)

    # field: tag  bits 16-23 (8 b)
    @property
    def tag(self):
        return _read_bits(self._parent_buf, self._p * 8 + 16, 8)
    @tag.setter
    def tag(self, v):
        _write_bits(self._parent_buf, self._p * 8 + 16, 8, v)

    # field: first_dw_be  bits 24-27 (4 b)
    @property
    def first_dw_be(self):
        return _read_bits(self._parent_buf, self._p * 8 + 24, 4)
    @first_dw_be.setter
    def first_dw_be(self, v):
        _write_bits(self._parent_buf, self._p * 8 + 24, 4, v)

    # field: last_dw_be  bits 28-31 (4 b)
    @property
    def last_dw_be(self):
        return _read_bits(self._parent_buf, self._p * 8 + 28, 4)
    @last_dw_be.setter
    def last_dw_be(self, v):
        _write_bits(self._parent_buf, self._p * 8 + 28, 4, v)

    # field: dest_id  bits 32-47 (16 b)
    @property
    def dest_id(self):
        return _read_bits(self._parent_buf, self._p * 8 + 32, 16)
    @dest_id.setter
    def dest_id(self, v):
        _write_bits(self._parent_buf, self._p * 8 + 32, 16, v)

    # field: ext_reg_num  bits 48-51 (4 b)
    @property
    def ext_reg_num(self):
        return _read_bits(self._parent_buf, self._p * 8 + 48, 4)
    @ext_reg_num.setter
    def ext_reg_num(self, v):
        _write_bits(self._parent_buf, self._p * 8 + 48, 4, v)

    # field: rsvd  bits 52-55 (4 b)
    @property
    def rsvd(self):
        return _read_bits(self._parent_buf, self._p * 8 + 52, 4)
    @rsvd.setter
    def rsvd(self, v):
        _write_bits(self._parent_buf, self._p * 8 + 52, 4, v)

    # field: r  bits 56-57 (2 b)
    @property
    def r(self):
        return _read_bits(self._parent_buf, self._p * 8 + 56, 2)
    @r.setter
    def r(self, v):
        _write_bits(self._parent_buf, self._p * 8 + 56, 2, v)

    # field: reg_num  bits 58-63 (6 b)
    @property
    def reg_num(self):
        return _read_bits(self._parent_buf, self._p * 8 + 58, 6)
    @reg_num.setter
    def reg_num(self, v):
        _write_bits(self._parent_buf, self._p * 8 + 58, 6, v)

    @classmethod
    def get_size(cls):
        return 8

    def __len__(self):
        return 8

    def __bytes__(self):
        return bytes(self._parent_buf[self._p : self._p + 8])

class CxlIoCompletionHeader:
    _size = 8

    def __init__(self):
        self._buf = bytearray(8)
        self._p = 0  # byte-offset into parent buffer (set by attach)
        self._parent_buf = self._buf  # default: own buffer

    def _attach(self, parent_buf: bytearray, offset: int):
        self._parent_buf = parent_buf
        self._p = offset

    # field: cpl_id  bits 0-15 (16 b)
    @property
    def cpl_id(self):
        return _read_bits(self._parent_buf, self._p * 8 + 0, 16)
    @cpl_id.setter
    def cpl_id(self, v):
        _write_bits(self._parent_buf, self._p * 8 + 0, 16, v)

    # field: byte_count_upper  bits 16-19 (4 b)
    @property
    def byte_count_upper(self):
        return _read_bits(self._parent_buf, self._p * 8 + 16, 4)
    @byte_count_upper.setter
    def byte_count_upper(self, v):
        _write_bits(self._parent_buf, self._p * 8 + 16, 4, v)

    # field: bcm  bits 20-20 (1 b)
    @property
    def bcm(self):
        return _read_bits(self._parent_buf, self._p * 8 + 20, 1)
    @bcm.setter
    def bcm(self, v):
        _write_bits(self._parent_buf, self._p * 8 + 20, 1, v)

    # field: status  bits 21-23 (3 b)
    @property
    def status(self):
        return _read_bits(self._parent_buf, self._p * 8 + 21, 3)
    @status.setter
    def status(self, v):
        _write_bits(self._parent_buf, self._p * 8 + 21, 3, v)

    # field: byte_count_lower  bits 24-31 (8 b)
    @property
    def byte_count_lower(self):
        return _read_bits(self._parent_buf, self._p * 8 + 24, 8)
    @byte_count_lower.setter
    def byte_count_lower(self, v):
        _write_bits(self._parent_buf, self._p * 8 + 24, 8, v)

    # field: req_id  bits 32-47 (16 b)
    @property
    def req_id(self):
        return _read_bits(self._parent_buf, self._p * 8 + 32, 16)
    @req_id.setter
    def req_id(self, v):
        _write_bits(self._parent_buf, self._p * 8 + 32, 16, v)

    # field: tag  bits 48-55 (8 b)
    @property
    def tag(self):
        return _read_bits(self._parent_buf, self._p * 8 + 48, 8)
    @tag.setter
    def tag(self, v):
        _write_bits(self._parent_buf, self._p * 8 + 48, 8, v)

    # field: lower_addr  bits 56-62 (7 b)
    @property
    def lower_addr(self):
        return _read_bits(self._parent_buf, self._p * 8 + 56, 7)
    @lower_addr.setter
    def lower_addr(self, v):
        _write_bits(self._parent_buf, self._p * 8 + 56, 7, v)

    # field: rsvd  bits 63-63 (1 b)
    @property
    def rsvd(self):
        return _read_bits(self._parent_buf, self._p * 8 + 63, 1)
    @rsvd.setter
    def rsvd(self, v):
        _write_bits(self._parent_buf, self._p * 8 + 63, 1, v)

    @classmethod
    def get_size(cls):
        return 8

    def __len__(self):
        return 8

    def __bytes__(self):
        return bytes(self._parent_buf[self._p : self._p + 8])

class CxlCacheHeader:
    _size = 2

    def __init__(self):
        self._buf = bytearray(2)
        self._p = 0  # byte-offset into parent buffer (set by attach)
        self._parent_buf = self._buf  # default: own buffer

    def _attach(self, parent_buf: bytearray, offset: int):
        self._parent_buf = parent_buf
        self._p = offset

    # field: port_index  bits 0-7 (8 b)
    @property
    def port_index(self):
        return _read_bits(self._parent_buf, self._p * 8 + 0, 8)
    @port_index.setter
    def port_index(self, v):
        _write_bits(self._parent_buf, self._p * 8 + 0, 8, v)

    # field: msg_class  bits 8-15 (8 b)
    @property
    def msg_class(self):
        return _read_bits(self._parent_buf, self._p * 8 + 8, 8)
    @msg_class.setter
    def msg_class(self, v):
        _write_bits(self._parent_buf, self._p * 8 + 8, 8, v)

    @classmethod
    def get_size(cls):
        return 2

    def __len__(self):
        return 2

    def __bytes__(self):
        return bytes(self._parent_buf[self._p : self._p + 2])

class CxlCacheD2HReqHeader:
    _size = 9

    def __init__(self):
        self._buf = bytearray(9)
        self._p = 0  # byte-offset into parent buffer (set by attach)
        self._parent_buf = self._buf  # default: own buffer

    def _attach(self, parent_buf: bytearray, offset: int):
        self._parent_buf = parent_buf
        self._p = offset

    # field: valid  bits 0-0 (1 b)
    @property
    def valid(self):
        return _read_bits(self._parent_buf, self._p * 8 + 0, 1)
    @valid.setter
    def valid(self, v):
        _write_bits(self._parent_buf, self._p * 8 + 0, 1, v)

    # field: cache_opcode  bits 1-5 (5 b)
    @property
    def cache_opcode(self):
        return _read_bits(self._parent_buf, self._p * 8 + 1, 5)
    @cache_opcode.setter
    def cache_opcode(self, v):
        _write_bits(self._parent_buf, self._p * 8 + 1, 5, v)

    # field: cqid  bits 6-17 (12 b)
    @property
    def cqid(self):
        return _read_bits(self._parent_buf, self._p * 8 + 6, 12)
    @cqid.setter
    def cqid(self, v):
        _write_bits(self._parent_buf, self._p * 8 + 6, 12, v)

    # field: nt  bits 18-18 (1 b)
    @property
    def nt(self):
        return _read_bits(self._parent_buf, self._p * 8 + 18, 1)
    @nt.setter
    def nt(self, v):
        _write_bits(self._parent_buf, self._p * 8 + 18, 1, v)

    # field: cache_id  bits 19-22 (4 b)
    @property
    def cache_id(self):
        return _read_bits(self._parent_buf, self._p * 8 + 19, 4)
    @cache_id.setter
    def cache_id(self, v):
        _write_bits(self._parent_buf, self._p * 8 + 19, 4, v)

    # field: addr  bits 23-68 (46 b)
    @property
    def addr(self):
        return _read_bits(self._parent_buf, self._p * 8 + 23, 46)
    @addr.setter
    def addr(self, v):
        _write_bits(self._parent_buf, self._p * 8 + 23, 46, v)

    # field: rsvd  bits 69-71 (3 b)
    @property
    def rsvd(self):
        return _read_bits(self._parent_buf, self._p * 8 + 69, 3)
    @rsvd.setter
    def rsvd(self, v):
        _write_bits(self._parent_buf, self._p * 8 + 69, 3, v)

    @classmethod
    def get_size(cls):
        return 9

    def __len__(self):
        return 9

    def __bytes__(self):
        return bytes(self._parent_buf[self._p : self._p + 9])

class CxlCacheD2HRspHeader:
    _size = 3

    def __init__(self):
        self._buf = bytearray(3)
        self._p = 0  # byte-offset into parent buffer (set by attach)
        self._parent_buf = self._buf  # default: own buffer

    def _attach(self, parent_buf: bytearray, offset: int):
        self._parent_buf = parent_buf
        self._p = offset

    # field: valid  bits 0-0 (1 b)
    @property
    def valid(self):
        return _read_bits(self._parent_buf, self._p * 8 + 0, 1)
    @valid.setter
    def valid(self, v):
        _write_bits(self._parent_buf, self._p * 8 + 0, 1, v)

    # field: cache_opcode  bits 1-5 (5 b)
    @property
    def cache_opcode(self):
        return _read_bits(self._parent_buf, self._p * 8 + 1, 5)
    @cache_opcode.setter
    def cache_opcode(self, v):
        _write_bits(self._parent_buf, self._p * 8 + 1, 5, v)

    # field: uqid  bits 6-17 (12 b)
    @property
    def uqid(self):
        return _read_bits(self._parent_buf, self._p * 8 + 6, 12)
    @uqid.setter
    def uqid(self, v):
        _write_bits(self._parent_buf, self._p * 8 + 6, 12, v)

    # field: rsvd  bits 18-23 (6 b)
    @property
    def rsvd(self):
        return _read_bits(self._parent_buf, self._p * 8 + 18, 6)
    @rsvd.setter
    def rsvd(self, v):
        _write_bits(self._parent_buf, self._p * 8 + 18, 6, v)

    @classmethod
    def get_size(cls):
        return 3

    def __len__(self):
        return 3

    def __bytes__(self):
        return bytes(self._parent_buf[self._p : self._p + 3])

class CxlCacheD2HDataHeader:
    _size = 3

    def __init__(self):
        self._buf = bytearray(3)
        self._p = 0  # byte-offset into parent buffer (set by attach)
        self._parent_buf = self._buf  # default: own buffer

    def _attach(self, parent_buf: bytearray, offset: int):
        self._parent_buf = parent_buf
        self._p = offset

    # field: valid  bits 0-0 (1 b)
    @property
    def valid(self):
        return _read_bits(self._parent_buf, self._p * 8 + 0, 1)
    @valid.setter
    def valid(self, v):
        _write_bits(self._parent_buf, self._p * 8 + 0, 1, v)

    # field: uqid  bits 1-12 (12 b)
    @property
    def uqid(self):
        return _read_bits(self._parent_buf, self._p * 8 + 1, 12)
    @uqid.setter
    def uqid(self, v):
        _write_bits(self._parent_buf, self._p * 8 + 1, 12, v)

    # field: bogus  bits 13-13 (1 b)
    @property
    def bogus(self):
        return _read_bits(self._parent_buf, self._p * 8 + 13, 1)
    @bogus.setter
    def bogus(self, v):
        _write_bits(self._parent_buf, self._p * 8 + 13, 1, v)

    # field: poison  bits 14-14 (1 b)
    @property
    def poison(self):
        return _read_bits(self._parent_buf, self._p * 8 + 14, 1)
    @poison.setter
    def poison(self, v):
        _write_bits(self._parent_buf, self._p * 8 + 14, 1, v)

    # field: bep  bits 15-15 (1 b)
    @property
    def bep(self):
        return _read_bits(self._parent_buf, self._p * 8 + 15, 1)
    @bep.setter
    def bep(self, v):
        _write_bits(self._parent_buf, self._p * 8 + 15, 1, v)

    # field: rsvd  bits 16-23 (8 b)
    @property
    def rsvd(self):
        return _read_bits(self._parent_buf, self._p * 8 + 16, 8)
    @rsvd.setter
    def rsvd(self, v):
        _write_bits(self._parent_buf, self._p * 8 + 16, 8, v)

    @classmethod
    def get_size(cls):
        return 3

    def __len__(self):
        return 3

    def __bytes__(self):
        return bytes(self._parent_buf[self._p : self._p + 3])

class CxlCacheH2DReqHeader:
    _size = 9

    def __init__(self):
        self._buf = bytearray(9)
        self._p = 0  # byte-offset into parent buffer (set by attach)
        self._parent_buf = self._buf  # default: own buffer

    def _attach(self, parent_buf: bytearray, offset: int):
        self._parent_buf = parent_buf
        self._p = offset

    # field: valid  bits 0-0 (1 b)
    @property
    def valid(self):
        return _read_bits(self._parent_buf, self._p * 8 + 0, 1)
    @valid.setter
    def valid(self, v):
        _write_bits(self._parent_buf, self._p * 8 + 0, 1, v)

    # field: cache_opcode  bits 1-3 (3 b)
    @property
    def cache_opcode(self):
        return _read_bits(self._parent_buf, self._p * 8 + 1, 3)
    @cache_opcode.setter
    def cache_opcode(self, v):
        _write_bits(self._parent_buf, self._p * 8 + 1, 3, v)

    # field: addr  bits 4-49 (46 b)
    @property
    def addr(self):
        return _read_bits(self._parent_buf, self._p * 8 + 4, 46)
    @addr.setter
    def addr(self, v):
        _write_bits(self._parent_buf, self._p * 8 + 4, 46, v)

    # field: uqid  bits 50-61 (12 b)
    @property
    def uqid(self):
        return _read_bits(self._parent_buf, self._p * 8 + 50, 12)
    @uqid.setter
    def uqid(self, v):
        _write_bits(self._parent_buf, self._p * 8 + 50, 12, v)

    # field: cache_id  bits 62-65 (4 b)
    @property
    def cache_id(self):
        return _read_bits(self._parent_buf, self._p * 8 + 62, 4)
    @cache_id.setter
    def cache_id(self, v):
        _write_bits(self._parent_buf, self._p * 8 + 62, 4, v)

    # field: rsvd  bits 66-71 (6 b)
    @property
    def rsvd(self):
        return _read_bits(self._parent_buf, self._p * 8 + 66, 6)
    @rsvd.setter
    def rsvd(self, v):
        _write_bits(self._parent_buf, self._p * 8 + 66, 6, v)

    @classmethod
    def get_size(cls):
        return 9

    def __len__(self):
        return 9

    def __bytes__(self):
        return bytes(self._parent_buf[self._p : self._p + 9])

class CxlCacheH2DRspHeader:
    _size = 5

    def __init__(self):
        self._buf = bytearray(5)
        self._p = 0  # byte-offset into parent buffer (set by attach)
        self._parent_buf = self._buf  # default: own buffer

    def _attach(self, parent_buf: bytearray, offset: int):
        self._parent_buf = parent_buf
        self._p = offset

    # field: valid  bits 0-0 (1 b)
    @property
    def valid(self):
        return _read_bits(self._parent_buf, self._p * 8 + 0, 1)
    @valid.setter
    def valid(self, v):
        _write_bits(self._parent_buf, self._p * 8 + 0, 1, v)

    # field: cache_opcode  bits 1-4 (4 b)
    @property
    def cache_opcode(self):
        return _read_bits(self._parent_buf, self._p * 8 + 1, 4)
    @cache_opcode.setter
    def cache_opcode(self, v):
        _write_bits(self._parent_buf, self._p * 8 + 1, 4, v)

    # field: rsp_data  bits 5-16 (12 b)
    @property
    def rsp_data(self):
        return _read_bits(self._parent_buf, self._p * 8 + 5, 12)
    @rsp_data.setter
    def rsp_data(self, v):
        _write_bits(self._parent_buf, self._p * 8 + 5, 12, v)

    # field: rsp_pre  bits 17-18 (2 b)
    @property
    def rsp_pre(self):
        return _read_bits(self._parent_buf, self._p * 8 + 17, 2)
    @rsp_pre.setter
    def rsp_pre(self, v):
        _write_bits(self._parent_buf, self._p * 8 + 17, 2, v)

    # field: cqid  bits 19-30 (12 b)
    @property
    def cqid(self):
        return _read_bits(self._parent_buf, self._p * 8 + 19, 12)
    @cqid.setter
    def cqid(self, v):
        _write_bits(self._parent_buf, self._p * 8 + 19, 12, v)

    # field: cache_id  bits 31-34 (4 b)
    @property
    def cache_id(self):
        return _read_bits(self._parent_buf, self._p * 8 + 31, 4)
    @cache_id.setter
    def cache_id(self, v):
        _write_bits(self._parent_buf, self._p * 8 + 31, 4, v)

    # field: rsvd  bits 35-39 (5 b)
    @property
    def rsvd(self):
        return _read_bits(self._parent_buf, self._p * 8 + 35, 5)
    @rsvd.setter
    def rsvd(self, v):
        _write_bits(self._parent_buf, self._p * 8 + 35, 5, v)

    @classmethod
    def get_size(cls):
        return 5

    def __len__(self):
        return 5

    def __bytes__(self):
        return bytes(self._parent_buf[self._p : self._p + 5])

class CxlCacheH2DDataHeader:
    _size = 3

    def __init__(self):
        self._buf = bytearray(3)
        self._p = 0  # byte-offset into parent buffer (set by attach)
        self._parent_buf = self._buf  # default: own buffer

    def _attach(self, parent_buf: bytearray, offset: int):
        self._parent_buf = parent_buf
        self._p = offset

    # field: valid  bits 0-0 (1 b)
    @property
    def valid(self):
        return _read_bits(self._parent_buf, self._p * 8 + 0, 1)
    @valid.setter
    def valid(self, v):
        _write_bits(self._parent_buf, self._p * 8 + 0, 1, v)

    # field: cqid  bits 1-12 (12 b)
    @property
    def cqid(self):
        return _read_bits(self._parent_buf, self._p * 8 + 1, 12)
    @cqid.setter
    def cqid(self, v):
        _write_bits(self._parent_buf, self._p * 8 + 1, 12, v)

    # field: poison  bits 13-13 (1 b)
    @property
    def poison(self):
        return _read_bits(self._parent_buf, self._p * 8 + 13, 1)
    @poison.setter
    def poison(self, v):
        _write_bits(self._parent_buf, self._p * 8 + 13, 1, v)

    # field: go_err  bits 14-14 (1 b)
    @property
    def go_err(self):
        return _read_bits(self._parent_buf, self._p * 8 + 14, 1)
    @go_err.setter
    def go_err(self, v):
        _write_bits(self._parent_buf, self._p * 8 + 14, 1, v)

    # field: cache_id  bits 15-18 (4 b)
    @property
    def cache_id(self):
        return _read_bits(self._parent_buf, self._p * 8 + 15, 4)
    @cache_id.setter
    def cache_id(self, v):
        _write_bits(self._parent_buf, self._p * 8 + 15, 4, v)

    # field: rsvd  bits 19-23 (5 b)
    @property
    def rsvd(self):
        return _read_bits(self._parent_buf, self._p * 8 + 19, 5)
    @rsvd.setter
    def rsvd(self, v):
        _write_bits(self._parent_buf, self._p * 8 + 19, 5, v)

    @classmethod
    def get_size(cls):
        return 3

    def __len__(self):
        return 3

    def __bytes__(self):
        return bytes(self._parent_buf[self._p : self._p + 3])

class CxlMemHeader:
    _size = 2

    def __init__(self):
        self._buf = bytearray(2)
        self._p = 0  # byte-offset into parent buffer (set by attach)
        self._parent_buf = self._buf  # default: own buffer

    def _attach(self, parent_buf: bytearray, offset: int):
        self._parent_buf = parent_buf
        self._p = offset

    # field: port_index  bits 0-7 (8 b)
    @property
    def port_index(self):
        return _read_bits(self._parent_buf, self._p * 8 + 0, 8)
    @port_index.setter
    def port_index(self, v):
        _write_bits(self._parent_buf, self._p * 8 + 0, 8, v)

    # field: msg_class  bits 8-15 (8 b)
    @property
    def msg_class(self):
        return _read_bits(self._parent_buf, self._p * 8 + 8, 8)
    @msg_class.setter
    def msg_class(self, v):
        _write_bits(self._parent_buf, self._p * 8 + 8, 8, v)

    @classmethod
    def get_size(cls):
        return 2

    def __len__(self):
        return 2

    def __bytes__(self):
        return bytes(self._parent_buf[self._p : self._p + 2])

class CxlMemM2SReqHeader:
    _size = 13

    def __init__(self):
        self._buf = bytearray(13)
        self._p = 0  # byte-offset into parent buffer (set by attach)
        self._parent_buf = self._buf  # default: own buffer

    def _attach(self, parent_buf: bytearray, offset: int):
        self._parent_buf = parent_buf
        self._p = offset

    # field: valid  bits 0-0 (1 b)
    @property
    def valid(self):
        return _read_bits(self._parent_buf, self._p * 8 + 0, 1)
    @valid.setter
    def valid(self, v):
        _write_bits(self._parent_buf, self._p * 8 + 0, 1, v)

    # field: mem_opcode  bits 1-4 (4 b)
    @property
    def mem_opcode(self):
        return _read_bits(self._parent_buf, self._p * 8 + 1, 4)
    @mem_opcode.setter
    def mem_opcode(self, v):
        _write_bits(self._parent_buf, self._p * 8 + 1, 4, v)

    # field: snp_type  bits 5-7 (3 b)
    @property
    def snp_type(self):
        return _read_bits(self._parent_buf, self._p * 8 + 5, 3)
    @snp_type.setter
    def snp_type(self, v):
        _write_bits(self._parent_buf, self._p * 8 + 5, 3, v)

    # field: meta_field  bits 8-9 (2 b)
    @property
    def meta_field(self):
        return _read_bits(self._parent_buf, self._p * 8 + 8, 2)
    @meta_field.setter
    def meta_field(self, v):
        _write_bits(self._parent_buf, self._p * 8 + 8, 2, v)

    # field: meta_value  bits 10-11 (2 b)
    @property
    def meta_value(self):
        return _read_bits(self._parent_buf, self._p * 8 + 10, 2)
    @meta_value.setter
    def meta_value(self, v):
        _write_bits(self._parent_buf, self._p * 8 + 10, 2, v)

    # field: tag  bits 12-27 (16 b)
    @property
    def tag(self):
        return _read_bits(self._parent_buf, self._p * 8 + 12, 16)
    @tag.setter
    def tag(self, v):
        _write_bits(self._parent_buf, self._p * 8 + 12, 16, v)

    # field: addr  bits 28-73 (46 b)
    @property
    def addr(self):
        return _read_bits(self._parent_buf, self._p * 8 + 28, 46)
    @addr.setter
    def addr(self, v):
        _write_bits(self._parent_buf, self._p * 8 + 28, 46, v)

    # field: ld_id  bits 74-77 (4 b)
    @property
    def ld_id(self):
        return _read_bits(self._parent_buf, self._p * 8 + 74, 4)
    @ld_id.setter
    def ld_id(self, v):
        _write_bits(self._parent_buf, self._p * 8 + 74, 4, v)

    # field: rsvd  bits 78-97 (20 b)
    @property
    def rsvd(self):
        return _read_bits(self._parent_buf, self._p * 8 + 78, 20)
    @rsvd.setter
    def rsvd(self, v):
        _write_bits(self._parent_buf, self._p * 8 + 78, 20, v)

    # field: tc  bits 98-99 (2 b)
    @property
    def tc(self):
        return _read_bits(self._parent_buf, self._p * 8 + 98, 2)
    @tc.setter
    def tc(self, v):
        _write_bits(self._parent_buf, self._p * 8 + 98, 2, v)

    # field: padding  bits 100-103 (4 b)
    @property
    def padding(self):
        return _read_bits(self._parent_buf, self._p * 8 + 100, 4)
    @padding.setter
    def padding(self, v):
        _write_bits(self._parent_buf, self._p * 8 + 100, 4, v)

    @classmethod
    def get_size(cls):
        return 13

    def __len__(self):
        return 13

    def __bytes__(self):
        return bytes(self._parent_buf[self._p : self._p + 13])

class CxlMemM2SRwDHeader:
    _size = 13

    def __init__(self):
        self._buf = bytearray(13)
        self._p = 0  # byte-offset into parent buffer (set by attach)
        self._parent_buf = self._buf  # default: own buffer

    def _attach(self, parent_buf: bytearray, offset: int):
        self._parent_buf = parent_buf
        self._p = offset

    # field: valid  bits 0-0 (1 b)
    @property
    def valid(self):
        return _read_bits(self._parent_buf, self._p * 8 + 0, 1)
    @valid.setter
    def valid(self, v):
        _write_bits(self._parent_buf, self._p * 8 + 0, 1, v)

    # field: mem_opcode  bits 1-4 (4 b)
    @property
    def mem_opcode(self):
        return _read_bits(self._parent_buf, self._p * 8 + 1, 4)
    @mem_opcode.setter
    def mem_opcode(self, v):
        _write_bits(self._parent_buf, self._p * 8 + 1, 4, v)

    # field: snp_type  bits 5-7 (3 b)
    @property
    def snp_type(self):
        return _read_bits(self._parent_buf, self._p * 8 + 5, 3)
    @snp_type.setter
    def snp_type(self, v):
        _write_bits(self._parent_buf, self._p * 8 + 5, 3, v)

    # field: meta_field  bits 8-9 (2 b)
    @property
    def meta_field(self):
        return _read_bits(self._parent_buf, self._p * 8 + 8, 2)
    @meta_field.setter
    def meta_field(self, v):
        _write_bits(self._parent_buf, self._p * 8 + 8, 2, v)

    # field: meta_value  bits 10-11 (2 b)
    @property
    def meta_value(self):
        return _read_bits(self._parent_buf, self._p * 8 + 10, 2)
    @meta_value.setter
    def meta_value(self, v):
        _write_bits(self._parent_buf, self._p * 8 + 10, 2, v)

    # field: tag  bits 12-27 (16 b)
    @property
    def tag(self):
        return _read_bits(self._parent_buf, self._p * 8 + 12, 16)
    @tag.setter
    def tag(self, v):
        _write_bits(self._parent_buf, self._p * 8 + 12, 16, v)

    # field: addr  bits 28-73 (46 b)
    @property
    def addr(self):
        return _read_bits(self._parent_buf, self._p * 8 + 28, 46)
    @addr.setter
    def addr(self, v):
        _write_bits(self._parent_buf, self._p * 8 + 28, 46, v)

    # field: poison  bits 74-74 (1 b)
    @property
    def poison(self):
        return _read_bits(self._parent_buf, self._p * 8 + 74, 1)
    @poison.setter
    def poison(self, v):
        _write_bits(self._parent_buf, self._p * 8 + 74, 1, v)

    # field: bep  bits 75-75 (1 b)
    @property
    def bep(self):
        return _read_bits(self._parent_buf, self._p * 8 + 75, 1)
    @bep.setter
    def bep(self, v):
        _write_bits(self._parent_buf, self._p * 8 + 75, 1, v)

    # field: ld_id  bits 76-79 (4 b)
    @property
    def ld_id(self):
        return _read_bits(self._parent_buf, self._p * 8 + 76, 4)
    @ld_id.setter
    def ld_id(self, v):
        _write_bits(self._parent_buf, self._p * 8 + 76, 4, v)

    # field: rsvd  bits 80-101 (22 b)
    @property
    def rsvd(self):
        return _read_bits(self._parent_buf, self._p * 8 + 80, 22)
    @rsvd.setter
    def rsvd(self, v):
        _write_bits(self._parent_buf, self._p * 8 + 80, 22, v)

    # field: tc  bits 102-103 (2 b)
    @property
    def tc(self):
        return _read_bits(self._parent_buf, self._p * 8 + 102, 2)
    @tc.setter
    def tc(self, v):
        _write_bits(self._parent_buf, self._p * 8 + 102, 2, v)

    @classmethod
    def get_size(cls):
        return 13

    def __len__(self):
        return 13

    def __bytes__(self):
        return bytes(self._parent_buf[self._p : self._p + 13])

class CxlMemM2SBIRspHeader:
    _size = 5

    def __init__(self):
        self._buf = bytearray(5)
        self._p = 0  # byte-offset into parent buffer (set by attach)
        self._parent_buf = self._buf  # default: own buffer

    def _attach(self, parent_buf: bytearray, offset: int):
        self._parent_buf = parent_buf
        self._p = offset

    # field: valid  bits 0-0 (1 b)
    @property
    def valid(self):
        return _read_bits(self._parent_buf, self._p * 8 + 0, 1)
    @valid.setter
    def valid(self, v):
        _write_bits(self._parent_buf, self._p * 8 + 0, 1, v)

    # field: opcode  bits 1-4 (4 b)
    @property
    def opcode(self):
        return _read_bits(self._parent_buf, self._p * 8 + 1, 4)
    @opcode.setter
    def opcode(self, v):
        _write_bits(self._parent_buf, self._p * 8 + 1, 4, v)

    # field: bi_id  bits 5-16 (12 b)
    @property
    def bi_id(self):
        return _read_bits(self._parent_buf, self._p * 8 + 5, 12)
    @bi_id.setter
    def bi_id(self, v):
        _write_bits(self._parent_buf, self._p * 8 + 5, 12, v)

    # field: bi_tag  bits 17-28 (12 b)
    @property
    def bi_tag(self):
        return _read_bits(self._parent_buf, self._p * 8 + 17, 12)
    @bi_tag.setter
    def bi_tag(self, v):
        _write_bits(self._parent_buf, self._p * 8 + 17, 12, v)

    # field: low_addr  bits 29-30 (2 b)
    @property
    def low_addr(self):
        return _read_bits(self._parent_buf, self._p * 8 + 29, 2)
    @low_addr.setter
    def low_addr(self, v):
        _write_bits(self._parent_buf, self._p * 8 + 29, 2, v)

    # field: rsvd  bits 31-39 (9 b)
    @property
    def rsvd(self):
        return _read_bits(self._parent_buf, self._p * 8 + 31, 9)
    @rsvd.setter
    def rsvd(self, v):
        _write_bits(self._parent_buf, self._p * 8 + 31, 9, v)

    @classmethod
    def get_size(cls):
        return 5

    def __len__(self):
        return 5

    def __bytes__(self):
        return bytes(self._parent_buf[self._p : self._p + 5])

class CxlMemS2MBISnpHeader:
    _size = 10

    def __init__(self):
        self._buf = bytearray(10)
        self._p = 0  # byte-offset into parent buffer (set by attach)
        self._parent_buf = self._buf  # default: own buffer

    def _attach(self, parent_buf: bytearray, offset: int):
        self._parent_buf = parent_buf
        self._p = offset

    # field: valid  bits 0-0 (1 b)
    @property
    def valid(self):
        return _read_bits(self._parent_buf, self._p * 8 + 0, 1)
    @valid.setter
    def valid(self, v):
        _write_bits(self._parent_buf, self._p * 8 + 0, 1, v)

    # field: opcode  bits 1-4 (4 b)
    @property
    def opcode(self):
        return _read_bits(self._parent_buf, self._p * 8 + 1, 4)
    @opcode.setter
    def opcode(self, v):
        _write_bits(self._parent_buf, self._p * 8 + 1, 4, v)

    # field: bi_id  bits 5-16 (12 b)
    @property
    def bi_id(self):
        return _read_bits(self._parent_buf, self._p * 8 + 5, 12)
    @bi_id.setter
    def bi_id(self, v):
        _write_bits(self._parent_buf, self._p * 8 + 5, 12, v)

    # field: bi_tag  bits 17-28 (12 b)
    @property
    def bi_tag(self):
        return _read_bits(self._parent_buf, self._p * 8 + 17, 12)
    @bi_tag.setter
    def bi_tag(self, v):
        _write_bits(self._parent_buf, self._p * 8 + 17, 12, v)

    # field: addr  bits 29-74 (46 b)
    @property
    def addr(self):
        return _read_bits(self._parent_buf, self._p * 8 + 29, 46)
    @addr.setter
    def addr(self, v):
        _write_bits(self._parent_buf, self._p * 8 + 29, 46, v)

    # field: rsvd  bits 75-79 (5 b)
    @property
    def rsvd(self):
        return _read_bits(self._parent_buf, self._p * 8 + 75, 5)
    @rsvd.setter
    def rsvd(self, v):
        _write_bits(self._parent_buf, self._p * 8 + 75, 5, v)

    @classmethod
    def get_size(cls):
        return 10

    def __len__(self):
        return 10

    def __bytes__(self):
        return bytes(self._parent_buf[self._p : self._p + 10])

class CxlMemS2MNDRHeader:
    _size = 5

    def __init__(self):
        self._buf = bytearray(5)
        self._p = 0  # byte-offset into parent buffer (set by attach)
        self._parent_buf = self._buf  # default: own buffer

    def _attach(self, parent_buf: bytearray, offset: int):
        self._parent_buf = parent_buf
        self._p = offset

    # field: valid  bits 0-0 (1 b)
    @property
    def valid(self):
        return _read_bits(self._parent_buf, self._p * 8 + 0, 1)
    @valid.setter
    def valid(self, v):
        _write_bits(self._parent_buf, self._p * 8 + 0, 1, v)

    # field: opcode  bits 1-3 (3 b)
    @property
    def opcode(self):
        return _read_bits(self._parent_buf, self._p * 8 + 1, 3)
    @opcode.setter
    def opcode(self, v):
        _write_bits(self._parent_buf, self._p * 8 + 1, 3, v)

    # field: meta_field  bits 4-5 (2 b)
    @property
    def meta_field(self):
        return _read_bits(self._parent_buf, self._p * 8 + 4, 2)
    @meta_field.setter
    def meta_field(self, v):
        _write_bits(self._parent_buf, self._p * 8 + 4, 2, v)

    # field: meta_value  bits 6-7 (2 b)
    @property
    def meta_value(self):
        return _read_bits(self._parent_buf, self._p * 8 + 6, 2)
    @meta_value.setter
    def meta_value(self, v):
        _write_bits(self._parent_buf, self._p * 8 + 6, 2, v)

    # field: tag  bits 8-23 (16 b)
    @property
    def tag(self):
        return _read_bits(self._parent_buf, self._p * 8 + 8, 16)
    @tag.setter
    def tag(self, v):
        _write_bits(self._parent_buf, self._p * 8 + 8, 16, v)

    # field: ld_id  bits 24-27 (4 b)
    @property
    def ld_id(self):
        return _read_bits(self._parent_buf, self._p * 8 + 24, 4)
    @ld_id.setter
    def ld_id(self, v):
        _write_bits(self._parent_buf, self._p * 8 + 24, 4, v)

    # field: dev_load  bits 28-29 (2 b)
    @property
    def dev_load(self):
        return _read_bits(self._parent_buf, self._p * 8 + 28, 2)
    @dev_load.setter
    def dev_load(self, v):
        _write_bits(self._parent_buf, self._p * 8 + 28, 2, v)

    # field: rsvd  bits 30-39 (10 b)
    @property
    def rsvd(self):
        return _read_bits(self._parent_buf, self._p * 8 + 30, 10)
    @rsvd.setter
    def rsvd(self, v):
        _write_bits(self._parent_buf, self._p * 8 + 30, 10, v)

    @classmethod
    def get_size(cls):
        return 5

    def __len__(self):
        return 5

    def __bytes__(self):
        return bytes(self._parent_buf[self._p : self._p + 5])

class CxlMemS2MDRSHeader:
    _size = 5

    def __init__(self):
        self._buf = bytearray(5)
        self._p = 0  # byte-offset into parent buffer (set by attach)
        self._parent_buf = self._buf  # default: own buffer

    def _attach(self, parent_buf: bytearray, offset: int):
        self._parent_buf = parent_buf
        self._p = offset

    # field: valid  bits 0-0 (1 b)
    @property
    def valid(self):
        return _read_bits(self._parent_buf, self._p * 8 + 0, 1)
    @valid.setter
    def valid(self, v):
        _write_bits(self._parent_buf, self._p * 8 + 0, 1, v)

    # field: opcode  bits 1-3 (3 b)
    @property
    def opcode(self):
        return _read_bits(self._parent_buf, self._p * 8 + 1, 3)
    @opcode.setter
    def opcode(self, v):
        _write_bits(self._parent_buf, self._p * 8 + 1, 3, v)

    # field: meta_field  bits 4-5 (2 b)
    @property
    def meta_field(self):
        return _read_bits(self._parent_buf, self._p * 8 + 4, 2)
    @meta_field.setter
    def meta_field(self, v):
        _write_bits(self._parent_buf, self._p * 8 + 4, 2, v)

    # field: meta_value  bits 6-7 (2 b)
    @property
    def meta_value(self):
        return _read_bits(self._parent_buf, self._p * 8 + 6, 2)
    @meta_value.setter
    def meta_value(self, v):
        _write_bits(self._parent_buf, self._p * 8 + 6, 2, v)

    # field: tag  bits 8-23 (16 b)
    @property
    def tag(self):
        return _read_bits(self._parent_buf, self._p * 8 + 8, 16)
    @tag.setter
    def tag(self, v):
        _write_bits(self._parent_buf, self._p * 8 + 8, 16, v)

    # field: poison  bits 24-24 (1 b)
    @property
    def poison(self):
        return _read_bits(self._parent_buf, self._p * 8 + 24, 1)
    @poison.setter
    def poison(self, v):
        _write_bits(self._parent_buf, self._p * 8 + 24, 1, v)

    # field: ld_id  bits 25-28 (4 b)
    @property
    def ld_id(self):
        return _read_bits(self._parent_buf, self._p * 8 + 25, 4)
    @ld_id.setter
    def ld_id(self, v):
        _write_bits(self._parent_buf, self._p * 8 + 25, 4, v)

    # field: dev_load  bits 29-30 (2 b)
    @property
    def dev_load(self):
        return _read_bits(self._parent_buf, self._p * 8 + 29, 2)
    @dev_load.setter
    def dev_load(self, v):
        _write_bits(self._parent_buf, self._p * 8 + 29, 2, v)

    # field: rsvd  bits 31-39 (9 b)
    @property
    def rsvd(self):
        return _read_bits(self._parent_buf, self._p * 8 + 31, 9)
    @rsvd.setter
    def rsvd(self, v):
        _write_bits(self._parent_buf, self._p * 8 + 31, 9, v)

    @classmethod
    def get_size(cls):
        return 5

    def __len__(self):
        return 5

    def __bytes__(self):
        return bytes(self._parent_buf[self._p : self._p + 5])

class CciHeader:
    _size = 2

    def __init__(self):
        self._buf = bytearray(2)
        self._p = 0  # byte-offset into parent buffer (set by attach)
        self._parent_buf = self._buf  # default: own buffer

    def _attach(self, parent_buf: bytearray, offset: int):
        self._parent_buf = parent_buf
        self._p = offset

    # field: port_index  bits 0-7 (8 b)
    @property
    def port_index(self):
        return _read_bits(self._parent_buf, self._p * 8 + 0, 8)
    @port_index.setter
    def port_index(self, v):
        _write_bits(self._parent_buf, self._p * 8 + 0, 8, v)

    # field: msg_class  bits 8-15 (8 b)
    @property
    def msg_class(self):
        return _read_bits(self._parent_buf, self._p * 8 + 8, 8)
    @msg_class.setter
    def msg_class(self, v):
        _write_bits(self._parent_buf, self._p * 8 + 8, 8, v)

    @classmethod
    def get_size(cls):
        return 2

    def __len__(self):
        return 2

    def __bytes__(self):
        return bytes(self._parent_buf[self._p : self._p + 2])

class CciMessageHeader:
    _size = 12

    def __init__(self):
        self._buf = bytearray(12)
        self._p = 0  # byte-offset into parent buffer (set by attach)
        self._parent_buf = self._buf  # default: own buffer

    def _attach(self, parent_buf: bytearray, offset: int):
        self._parent_buf = parent_buf
        self._p = offset

    # field: message_category  bits 0-3 (4 b)
    @property
    def message_category(self):
        return _read_bits(self._parent_buf, self._p * 8 + 0, 4)
    @message_category.setter
    def message_category(self, v):
        _write_bits(self._parent_buf, self._p * 8 + 0, 4, v)

    # field: reserved0  bits 4-7 (4 b)
    @property
    def reserved0(self):
        return _read_bits(self._parent_buf, self._p * 8 + 4, 4)
    @reserved0.setter
    def reserved0(self, v):
        _write_bits(self._parent_buf, self._p * 8 + 4, 4, v)

    # field: message_tag  bits 8-15 (8 b)
    @property
    def message_tag(self):
        return _read_bits(self._parent_buf, self._p * 8 + 8, 8)
    @message_tag.setter
    def message_tag(self, v):
        _write_bits(self._parent_buf, self._p * 8 + 8, 8, v)

    # field: reserved1  bits 16-23 (8 b)
    @property
    def reserved1(self):
        return _read_bits(self._parent_buf, self._p * 8 + 16, 8)
    @reserved1.setter
    def reserved1(self, v):
        _write_bits(self._parent_buf, self._p * 8 + 16, 8, v)

    # field: command_opcode  bits 24-39 (16 b)
    @property
    def command_opcode(self):
        return _read_bits(self._parent_buf, self._p * 8 + 24, 16)
    @command_opcode.setter
    def command_opcode(self, v):
        _write_bits(self._parent_buf, self._p * 8 + 24, 16, v)

    # field: message_payload_length_low  bits 40-55 (16 b)
    @property
    def message_payload_length_low(self):
        return _read_bits(self._parent_buf, self._p * 8 + 40, 16)
    @message_payload_length_low.setter
    def message_payload_length_low(self, v):
        _write_bits(self._parent_buf, self._p * 8 + 40, 16, v)

    # field: message_payload_length_high  bits 56-60 (5 b)
    @property
    def message_payload_length_high(self):
        return _read_bits(self._parent_buf, self._p * 8 + 56, 5)
    @message_payload_length_high.setter
    def message_payload_length_high(self, v):
        _write_bits(self._parent_buf, self._p * 8 + 56, 5, v)

    # field: reserved2  bits 61-62 (2 b)
    @property
    def reserved2(self):
        return _read_bits(self._parent_buf, self._p * 8 + 61, 2)
    @reserved2.setter
    def reserved2(self, v):
        _write_bits(self._parent_buf, self._p * 8 + 61, 2, v)

    # field: background_operation  bits 63-63 (1 b)
    @property
    def background_operation(self):
        return _read_bits(self._parent_buf, self._p * 8 + 63, 1)
    @background_operation.setter
    def background_operation(self, v):
        _write_bits(self._parent_buf, self._p * 8 + 63, 1, v)

    # field: return_code  bits 64-79 (16 b)
    @property
    def return_code(self):
        return _read_bits(self._parent_buf, self._p * 8 + 64, 16)
    @return_code.setter
    def return_code(self, v):
        _write_bits(self._parent_buf, self._p * 8 + 64, 16, v)

    # field: vendor_specific_extended_status  bits 80-95 (16 b)
    @property
    def vendor_specific_extended_status(self):
        return _read_bits(self._parent_buf, self._p * 8 + 80, 16)
    @vendor_specific_extended_status.setter
    def vendor_specific_extended_status(self, v):
        _write_bits(self._parent_buf, self._p * 8 + 80, 16, v)

    @classmethod
    def get_size(cls):
        return 12

    def __len__(self):
        return 12

    def __bytes__(self):
        return bytes(self._parent_buf[self._p : self._p + 12])

class PbrHeader:
    _size = 4

    def __init__(self):
        self._buf = bytearray(4)
        self._p = 0  # byte-offset into parent buffer (set by attach)
        self._parent_buf = self._buf  # default: own buffer

    def _attach(self, parent_buf: bytearray, offset: int):
        self._parent_buf = parent_buf
        self._p = offset

    # field: spid  bits 0-11 (12 b)
    @property
    def spid(self):
        return _read_bits(self._parent_buf, self._p * 8 + 0, 12)
    @spid.setter
    def spid(self, v):
        _write_bits(self._parent_buf, self._p * 8 + 0, 12, v)

    # field: dpid  bits 12-23 (12 b)
    @property
    def dpid(self):
        return _read_bits(self._parent_buf, self._p * 8 + 12, 12)
    @dpid.setter
    def dpid(self, v):
        _write_bits(self._parent_buf, self._p * 8 + 12, 12, v)

    # field: reserved  bits 24-31 (8 b)
    @property
    def reserved(self):
        return _read_bits(self._parent_buf, self._p * 8 + 24, 8)
    @reserved.setter
    def reserved(self, v):
        _write_bits(self._parent_buf, self._p * 8 + 24, 8, v)

    @classmethod
    def get_size(cls):
        return 4

    def __len__(self):
        return 4

    def __bytes__(self):
        return bytes(self._parent_buf[self._p : self._p + 4])

# ---------------------------------------------------------------------------
# Composite packet classes (one class per packets.py entry)
# ---------------------------------------------------------------------------

class _GenBasePacket:
    _header_size = 2

    def __init__(self, payload=None):
        self._buf = bytearray(512)
        self._data_length = 0
        self._system_header = SystemHeader()
        self._system_header._attach(self._buf, 0)
        if payload is not None:
            n = len(payload)
            if n > 512:
                raise ValueError("packet too large")
            self._buf[:n] = payload
            self._data_length = n - 2

    @property
    def system_header(self):
        return self._system_header

    def set_data(self, data):
        n = len(data)
        if 2 + n > 512:
            raise ValueError("packet too large")
        self._buf[2:2+n] = data
        self._data_length = n

    def get_data(self):
        return bytes(self._buf[2:2+self._data_length])

    def get_payload_offset(self):
        return 2

    def get_size(self):
        return 2 + self._data_length

    def get_byte_offset(self, other):
        if isinstance(other, SystemHeader):
            return 0
        raise TypeError(f'unsupported header type: {type(other)}')

    def set_bytes(self, offset, data):
        n = len(data)
        self._buf[offset:offset+n] = data

    def write_bits(self, start_bit, width, value):
        _write_bits(self._buf, start_bit, width, value)

    def read_bits(self, start_bit, width):
        return _read_bits(self._buf, start_bit, width)

    def get_bytes(self, start, length):
        return bytes(self._buf[start:start+length])

    def __len__(self):
        return 2 + self._data_length

    def __bytes__(self):
        return bytes(self._buf[:2 + self._data_length])

    def __iter__(self):
        return iter(self._buf[:2 + self._data_length])

    def __getitem__(self, key):
        return self._buf[:2 + self._data_length][key]

    def __enter__(self):
        return self

    def __exit__(self, *a):
        pass

BasePacket = _GenBasePacket

class _GenBaseSidebandPacket:
    _header_size = 3

    def __init__(self, payload=None):
        self._buf = bytearray(512)
        self._data_length = 0
        self._system_header = SystemHeader()
        self._system_header._attach(self._buf, 0)
        self._sideband_header = SidebandHeader()
        self._sideband_header._attach(self._buf, 2)
        if payload is not None:
            n = len(payload)
            if n > 512:
                raise ValueError("packet too large")
            self._buf[:n] = payload
            self._data_length = n - 3

    @property
    def system_header(self):
        return self._system_header

    @property
    def sideband_header(self):
        return self._sideband_header

    def set_data(self, data):
        n = len(data)
        if 3 + n > 512:
            raise ValueError("packet too large")
        self._buf[3:3+n] = data
        self._data_length = n

    def get_data(self):
        return bytes(self._buf[3:3+self._data_length])

    def get_payload_offset(self):
        return 3

    def get_size(self):
        return 3 + self._data_length

    def get_byte_offset(self, other):
        if isinstance(other, SystemHeader):
            return 0
        elif isinstance(other, SidebandHeader):
            return 2
        raise TypeError(f'unsupported header type: {type(other)}')

    def set_bytes(self, offset, data):
        n = len(data)
        self._buf[offset:offset+n] = data

    def write_bits(self, start_bit, width, value):
        _write_bits(self._buf, start_bit, width, value)

    def read_bits(self, start_bit, width):
        return _read_bits(self._buf, start_bit, width)

    def get_bytes(self, start, length):
        return bytes(self._buf[start:start+length])

    def __len__(self):
        return 3 + self._data_length

    def __bytes__(self):
        return bytes(self._buf[:3 + self._data_length])

    def __iter__(self):
        return iter(self._buf[:3 + self._data_length])

    def __getitem__(self, key):
        return self._buf[:3 + self._data_length][key]

    def __enter__(self):
        return self

    def __exit__(self, *a):
        pass

BaseSidebandPacket = _GenBaseSidebandPacket

class _GenSidebandConnectionRequestPacket:
    _header_size = 3

    def __init__(self, payload=None):
        self._buf = bytearray(512)
        self._data_length = 0
        self._system_header = SystemHeader()
        self._system_header._attach(self._buf, 0)
        self._sideband_header = SidebandHeader()
        self._sideband_header._attach(self._buf, 2)
        if payload is not None:
            n = len(payload)
            if n > 512:
                raise ValueError("packet too large")
            self._buf[:n] = payload
            self._data_length = n - 3

    @property
    def system_header(self):
        return self._system_header

    @property
    def sideband_header(self):
        return self._sideband_header

    def set_data(self, data):
        n = len(data)
        if 3 + n > 512:
            raise ValueError("packet too large")
        self._buf[3:3+n] = data
        self._data_length = n

    def get_data(self):
        return bytes(self._buf[3:3+self._data_length])

    def get_payload_offset(self):
        return 3

    def get_size(self):
        return 3 + self._data_length

    def get_byte_offset(self, other):
        if isinstance(other, SystemHeader):
            return 0
        elif isinstance(other, SidebandHeader):
            return 2
        raise TypeError(f'unsupported header type: {type(other)}')

    def set_bytes(self, offset, data):
        n = len(data)
        self._buf[offset:offset+n] = data

    def write_bits(self, start_bit, width, value):
        _write_bits(self._buf, start_bit, width, value)

    def read_bits(self, start_bit, width):
        return _read_bits(self._buf, start_bit, width)

    def get_bytes(self, start, length):
        return bytes(self._buf[start:start+length])

    def __len__(self):
        return 3 + self._data_length

    def __bytes__(self):
        return bytes(self._buf[:3 + self._data_length])

    def __iter__(self):
        return iter(self._buf[:3 + self._data_length])

    def __getitem__(self, key):
        return self._buf[:3 + self._data_length][key]

    def __enter__(self):
        return self

    def __exit__(self, *a):
        pass

SidebandConnectionRequestPacket = _GenSidebandConnectionRequestPacket

class _GenCxlIoBasePacket:
    _header_size = 10

    def __init__(self, payload=None):
        self._buf = bytearray(512)
        self._data_length = 0
        self._system_header = SystemHeader()
        self._system_header._attach(self._buf, 0)
        self._tlp_prefix = TlpPrefix()
        self._tlp_prefix._attach(self._buf, 2)
        self._cxl_io_header = CxlIoHeader()
        self._cxl_io_header._attach(self._buf, 6)
        if payload is not None:
            n = len(payload)
            if n > 512:
                raise ValueError("packet too large")
            self._buf[:n] = payload
            self._data_length = n - 10

    @property
    def system_header(self):
        return self._system_header

    @property
    def tlp_prefix(self):
        return self._tlp_prefix

    @property
    def cxl_io_header(self):
        return self._cxl_io_header

    def set_data(self, data):
        n = len(data)
        if 10 + n > 512:
            raise ValueError("packet too large")
        self._buf[10:10+n] = data
        self._data_length = n

    def get_data(self):
        return bytes(self._buf[10:10+self._data_length])

    def get_payload_offset(self):
        return 10

    def get_size(self):
        return 10 + self._data_length

    def get_byte_offset(self, other):
        if isinstance(other, SystemHeader):
            return 0
        elif isinstance(other, TlpPrefix):
            return 2
        elif isinstance(other, CxlIoHeader):
            return 6
        raise TypeError(f'unsupported header type: {type(other)}')

    def set_bytes(self, offset, data):
        n = len(data)
        self._buf[offset:offset+n] = data

    def write_bits(self, start_bit, width, value):
        _write_bits(self._buf, start_bit, width, value)

    def read_bits(self, start_bit, width):
        return _read_bits(self._buf, start_bit, width)

    def get_bytes(self, start, length):
        return bytes(self._buf[start:start+length])

    def __len__(self):
        return 10 + self._data_length

    def __bytes__(self):
        return bytes(self._buf[:10 + self._data_length])

    def __iter__(self):
        return iter(self._buf[:10 + self._data_length])

    def __getitem__(self, key):
        return self._buf[:10 + self._data_length][key]

    def __enter__(self):
        return self

    def __exit__(self, *a):
        pass

CxlIoBasePacket = _GenCxlIoBasePacket

class _GenCxlIoMemRdPacket:
    _header_size = 22

    def __init__(self, payload=None):
        self._buf = bytearray(512)
        self._data_length = 0
        self._system_header = SystemHeader()
        self._system_header._attach(self._buf, 0)
        self._tlp_prefix = TlpPrefix()
        self._tlp_prefix._attach(self._buf, 2)
        self._cxl_io_header = CxlIoHeader()
        self._cxl_io_header._attach(self._buf, 6)
        self._mreq_header = CxlIoMReqHeader()
        self._mreq_header._attach(self._buf, 10)
        if payload is not None:
            n = len(payload)
            if n > 512:
                raise ValueError("packet too large")
            self._buf[:n] = payload
            self._data_length = n - 22

    @property
    def system_header(self):
        return self._system_header

    @property
    def tlp_prefix(self):
        return self._tlp_prefix

    @property
    def cxl_io_header(self):
        return self._cxl_io_header

    @property
    def mreq_header(self):
        return self._mreq_header

    @classmethod
    def create(cls, system_header__payload_type, tlp_prefix__ld_id, cxl_io_header__fmt_type, cxl_io_header__length_upper, cxl_io_header__length_lower, mreq_header__req_id, mreq_header__tag, mreq_header__first_dw_be, mreq_header__last_dw_be, mreq_header__addr_upper, mreq_header__addr_lower, data: bytes = None):
        pkt = cls()
        pkt._buf = bytearray(512)
        pkt._system_header._attach(pkt._buf, 0)
        pkt._tlp_prefix._attach(pkt._buf, 2)
        pkt._cxl_io_header._attach(pkt._buf, 6)
        pkt._mreq_header._attach(pkt._buf, 10)
        pkt._system_header.payload_type = system_header__payload_type
        pkt._tlp_prefix.ld_id = tlp_prefix__ld_id
        pkt._cxl_io_header.fmt_type = cxl_io_header__fmt_type
        pkt._cxl_io_header.length_upper = cxl_io_header__length_upper
        pkt._cxl_io_header.length_lower = cxl_io_header__length_lower
        pkt._mreq_header.req_id = mreq_header__req_id
        pkt._mreq_header.tag = mreq_header__tag
        pkt._mreq_header.first_dw_be = mreq_header__first_dw_be
        pkt._mreq_header.last_dw_be = mreq_header__last_dw_be
        pkt._mreq_header.addr_upper = mreq_header__addr_upper
        pkt._mreq_header.addr_lower = mreq_header__addr_lower
        if data is not None:
            ptr = 22
            n = len(data)
            if 22 + n > 512:
                raise ValueError("data too large")
            pkt._buf[ptr:ptr+n] = data
            pkt._data_length = n
        pkt._system_header.payload_length = 22 + pkt._data_length
        return pkt

    def assign(self, system_header__payload_type, tlp_prefix__ld_id, cxl_io_header__fmt_type, cxl_io_header__length_upper, cxl_io_header__length_lower, mreq_header__req_id, mreq_header__tag, mreq_header__first_dw_be, mreq_header__last_dw_be, mreq_header__addr_upper, mreq_header__addr_lower, data: bytes = None):
        self._buf = bytearray(512)
        self._system_header._attach(self._buf, 0)
        self._tlp_prefix._attach(self._buf, 2)
        self._cxl_io_header._attach(self._buf, 6)
        self._mreq_header._attach(self._buf, 10)
        self._system_header.payload_type = system_header__payload_type
        self._tlp_prefix.ld_id = tlp_prefix__ld_id
        self._cxl_io_header.fmt_type = cxl_io_header__fmt_type
        self._cxl_io_header.length_upper = cxl_io_header__length_upper
        self._cxl_io_header.length_lower = cxl_io_header__length_lower
        self._mreq_header.req_id = mreq_header__req_id
        self._mreq_header.tag = mreq_header__tag
        self._mreq_header.first_dw_be = mreq_header__first_dw_be
        self._mreq_header.last_dw_be = mreq_header__last_dw_be
        self._mreq_header.addr_upper = mreq_header__addr_upper
        self._mreq_header.addr_lower = mreq_header__addr_lower
        if data is not None:
            ptr = 22
            n = len(data)
            self._buf[ptr:ptr+n] = data
            self._data_length = n
        self._system_header.payload_length = 22 + self._data_length

    def set_data(self, data):
        n = len(data)
        if 22 + n > 512:
            raise ValueError("packet too large")
        self._buf[22:22+n] = data
        self._data_length = n

    def get_data(self):
        return bytes(self._buf[22:22+self._data_length])

    def get_payload_offset(self):
        return 22

    def get_size(self):
        return 22 + self._data_length

    def get_byte_offset(self, other):
        if isinstance(other, SystemHeader):
            return 0
        elif isinstance(other, TlpPrefix):
            return 2
        elif isinstance(other, CxlIoHeader):
            return 6
        elif isinstance(other, CxlIoMReqHeader):
            return 10
        raise TypeError(f'unsupported header type: {type(other)}')

    def set_bytes(self, offset, data):
        n = len(data)
        self._buf[offset:offset+n] = data

    def write_bits(self, start_bit, width, value):
        _write_bits(self._buf, start_bit, width, value)

    def read_bits(self, start_bit, width):
        return _read_bits(self._buf, start_bit, width)

    def get_bytes(self, start, length):
        return bytes(self._buf[start:start+length])

    def __len__(self):
        return 22 + self._data_length

    def __bytes__(self):
        return bytes(self._buf[:22 + self._data_length])

    def __iter__(self):
        return iter(self._buf[:22 + self._data_length])

    def __getitem__(self, key):
        return self._buf[:22 + self._data_length][key]

    def __enter__(self):
        return self

    def __exit__(self, *a):
        pass

CxlIoMemRdPacket = _GenCxlIoMemRdPacket

class _GenCxlIoMemWrPacket:
    _header_size = 22

    def __init__(self, payload=None):
        self._buf = bytearray(512)
        self._data_length = 0
        self._system_header = SystemHeader()
        self._system_header._attach(self._buf, 0)
        self._tlp_prefix = TlpPrefix()
        self._tlp_prefix._attach(self._buf, 2)
        self._cxl_io_header = CxlIoHeader()
        self._cxl_io_header._attach(self._buf, 6)
        self._mreq_header = CxlIoMReqHeader()
        self._mreq_header._attach(self._buf, 10)
        if payload is not None:
            n = len(payload)
            if n > 512:
                raise ValueError("packet too large")
            self._buf[:n] = payload
            self._data_length = n - 22

    @property
    def system_header(self):
        return self._system_header

    @property
    def tlp_prefix(self):
        return self._tlp_prefix

    @property
    def cxl_io_header(self):
        return self._cxl_io_header

    @property
    def mreq_header(self):
        return self._mreq_header

    @classmethod
    def create(cls, system_header__payload_type, tlp_prefix__ld_id, cxl_io_header__fmt_type, cxl_io_header__length_upper, cxl_io_header__length_lower, mreq_header__req_id, mreq_header__tag, mreq_header__first_dw_be, mreq_header__last_dw_be, mreq_header__addr_upper, mreq_header__addr_lower, data: bytes = None):
        pkt = cls()
        pkt._buf = bytearray(512)
        pkt._system_header._attach(pkt._buf, 0)
        pkt._tlp_prefix._attach(pkt._buf, 2)
        pkt._cxl_io_header._attach(pkt._buf, 6)
        pkt._mreq_header._attach(pkt._buf, 10)
        pkt._system_header.payload_type = system_header__payload_type
        pkt._tlp_prefix.ld_id = tlp_prefix__ld_id
        pkt._cxl_io_header.fmt_type = cxl_io_header__fmt_type
        pkt._cxl_io_header.length_upper = cxl_io_header__length_upper
        pkt._cxl_io_header.length_lower = cxl_io_header__length_lower
        pkt._mreq_header.req_id = mreq_header__req_id
        pkt._mreq_header.tag = mreq_header__tag
        pkt._mreq_header.first_dw_be = mreq_header__first_dw_be
        pkt._mreq_header.last_dw_be = mreq_header__last_dw_be
        pkt._mreq_header.addr_upper = mreq_header__addr_upper
        pkt._mreq_header.addr_lower = mreq_header__addr_lower
        if data is not None:
            ptr = 22
            n = len(data)
            if 22 + n > 512:
                raise ValueError("data too large")
            pkt._buf[ptr:ptr+n] = data
            pkt._data_length = n
        pkt._system_header.payload_length = 22 + pkt._data_length
        return pkt

    def assign(self, system_header__payload_type, tlp_prefix__ld_id, cxl_io_header__fmt_type, cxl_io_header__length_upper, cxl_io_header__length_lower, mreq_header__req_id, mreq_header__tag, mreq_header__first_dw_be, mreq_header__last_dw_be, mreq_header__addr_upper, mreq_header__addr_lower, data: bytes = None):
        self._buf = bytearray(512)
        self._system_header._attach(self._buf, 0)
        self._tlp_prefix._attach(self._buf, 2)
        self._cxl_io_header._attach(self._buf, 6)
        self._mreq_header._attach(self._buf, 10)
        self._system_header.payload_type = system_header__payload_type
        self._tlp_prefix.ld_id = tlp_prefix__ld_id
        self._cxl_io_header.fmt_type = cxl_io_header__fmt_type
        self._cxl_io_header.length_upper = cxl_io_header__length_upper
        self._cxl_io_header.length_lower = cxl_io_header__length_lower
        self._mreq_header.req_id = mreq_header__req_id
        self._mreq_header.tag = mreq_header__tag
        self._mreq_header.first_dw_be = mreq_header__first_dw_be
        self._mreq_header.last_dw_be = mreq_header__last_dw_be
        self._mreq_header.addr_upper = mreq_header__addr_upper
        self._mreq_header.addr_lower = mreq_header__addr_lower
        if data is not None:
            ptr = 22
            n = len(data)
            self._buf[ptr:ptr+n] = data
            self._data_length = n
        self._system_header.payload_length = 22 + self._data_length

    def set_data(self, data):
        n = len(data)
        if 22 + n > 512:
            raise ValueError("packet too large")
        self._buf[22:22+n] = data
        self._data_length = n

    def get_data(self):
        return bytes(self._buf[22:22+self._data_length])

    def get_payload_offset(self):
        return 22

    def get_size(self):
        return 22 + self._data_length

    def get_byte_offset(self, other):
        if isinstance(other, SystemHeader):
            return 0
        elif isinstance(other, TlpPrefix):
            return 2
        elif isinstance(other, CxlIoHeader):
            return 6
        elif isinstance(other, CxlIoMReqHeader):
            return 10
        raise TypeError(f'unsupported header type: {type(other)}')

    def set_bytes(self, offset, data):
        n = len(data)
        self._buf[offset:offset+n] = data

    def write_bits(self, start_bit, width, value):
        _write_bits(self._buf, start_bit, width, value)

    def read_bits(self, start_bit, width):
        return _read_bits(self._buf, start_bit, width)

    def get_bytes(self, start, length):
        return bytes(self._buf[start:start+length])

    def __len__(self):
        return 22 + self._data_length

    def __bytes__(self):
        return bytes(self._buf[:22 + self._data_length])

    def __iter__(self):
        return iter(self._buf[:22 + self._data_length])

    def __getitem__(self, key):
        return self._buf[:22 + self._data_length][key]

    def __enter__(self):
        return self

    def __exit__(self, *a):
        pass

CxlIoMemWrPacket = _GenCxlIoMemWrPacket

class _GenCxlIoCfgRdPacket:
    _header_size = 18

    def __init__(self, payload=None):
        self._buf = bytearray(512)
        self._data_length = 0
        self._system_header = SystemHeader()
        self._system_header._attach(self._buf, 0)
        self._tlp_prefix = TlpPrefix()
        self._tlp_prefix._attach(self._buf, 2)
        self._cxl_io_header = CxlIoHeader()
        self._cxl_io_header._attach(self._buf, 6)
        self._cfg_req_header = CxlIoCfgReqHeader()
        self._cfg_req_header._attach(self._buf, 10)
        if payload is not None:
            n = len(payload)
            if n > 512:
                raise ValueError("packet too large")
            self._buf[:n] = payload
            self._data_length = n - 18

    @property
    def system_header(self):
        return self._system_header

    @property
    def tlp_prefix(self):
        return self._tlp_prefix

    @property
    def cxl_io_header(self):
        return self._cxl_io_header

    @property
    def cfg_req_header(self):
        return self._cfg_req_header

    @classmethod
    def create(cls, system_header__payload_type, tlp_prefix__ld_id, cxl_io_header__fmt_type, cxl_io_header__length_upper, cxl_io_header__length_lower, cfg_req_header__req_id, cfg_req_header__tag, cfg_req_header__first_dw_be, cfg_req_header__last_dw_be, cfg_req_header__dest_id, cfg_req_header__ext_reg_num, cfg_req_header__reg_num, data: bytes = None):
        pkt = cls()
        pkt._buf = bytearray(512)
        pkt._system_header._attach(pkt._buf, 0)
        pkt._tlp_prefix._attach(pkt._buf, 2)
        pkt._cxl_io_header._attach(pkt._buf, 6)
        pkt._cfg_req_header._attach(pkt._buf, 10)
        pkt._system_header.payload_type = system_header__payload_type
        pkt._tlp_prefix.ld_id = tlp_prefix__ld_id
        pkt._cxl_io_header.fmt_type = cxl_io_header__fmt_type
        pkt._cxl_io_header.length_upper = cxl_io_header__length_upper
        pkt._cxl_io_header.length_lower = cxl_io_header__length_lower
        pkt._cfg_req_header.req_id = cfg_req_header__req_id
        pkt._cfg_req_header.tag = cfg_req_header__tag
        pkt._cfg_req_header.first_dw_be = cfg_req_header__first_dw_be
        pkt._cfg_req_header.last_dw_be = cfg_req_header__last_dw_be
        pkt._cfg_req_header.dest_id = cfg_req_header__dest_id
        pkt._cfg_req_header.ext_reg_num = cfg_req_header__ext_reg_num
        pkt._cfg_req_header.reg_num = cfg_req_header__reg_num
        if data is not None:
            ptr = 18
            n = len(data)
            if 18 + n > 512:
                raise ValueError("data too large")
            pkt._buf[ptr:ptr+n] = data
            pkt._data_length = n
        pkt._system_header.payload_length = 18 + pkt._data_length
        return pkt

    def assign(self, system_header__payload_type, tlp_prefix__ld_id, cxl_io_header__fmt_type, cxl_io_header__length_upper, cxl_io_header__length_lower, cfg_req_header__req_id, cfg_req_header__tag, cfg_req_header__first_dw_be, cfg_req_header__last_dw_be, cfg_req_header__dest_id, cfg_req_header__ext_reg_num, cfg_req_header__reg_num, data: bytes = None):
        self._buf = bytearray(512)
        self._system_header._attach(self._buf, 0)
        self._tlp_prefix._attach(self._buf, 2)
        self._cxl_io_header._attach(self._buf, 6)
        self._cfg_req_header._attach(self._buf, 10)
        self._system_header.payload_type = system_header__payload_type
        self._tlp_prefix.ld_id = tlp_prefix__ld_id
        self._cxl_io_header.fmt_type = cxl_io_header__fmt_type
        self._cxl_io_header.length_upper = cxl_io_header__length_upper
        self._cxl_io_header.length_lower = cxl_io_header__length_lower
        self._cfg_req_header.req_id = cfg_req_header__req_id
        self._cfg_req_header.tag = cfg_req_header__tag
        self._cfg_req_header.first_dw_be = cfg_req_header__first_dw_be
        self._cfg_req_header.last_dw_be = cfg_req_header__last_dw_be
        self._cfg_req_header.dest_id = cfg_req_header__dest_id
        self._cfg_req_header.ext_reg_num = cfg_req_header__ext_reg_num
        self._cfg_req_header.reg_num = cfg_req_header__reg_num
        if data is not None:
            ptr = 18
            n = len(data)
            self._buf[ptr:ptr+n] = data
            self._data_length = n
        self._system_header.payload_length = 18 + self._data_length

    def set_data(self, data):
        n = len(data)
        if 18 + n > 512:
            raise ValueError("packet too large")
        self._buf[18:18+n] = data
        self._data_length = n

    def get_data(self):
        return bytes(self._buf[18:18+self._data_length])

    def get_payload_offset(self):
        return 18

    def get_size(self):
        return 18 + self._data_length

    def get_byte_offset(self, other):
        if isinstance(other, SystemHeader):
            return 0
        elif isinstance(other, TlpPrefix):
            return 2
        elif isinstance(other, CxlIoHeader):
            return 6
        elif isinstance(other, CxlIoCfgReqHeader):
            return 10
        raise TypeError(f'unsupported header type: {type(other)}')

    def set_bytes(self, offset, data):
        n = len(data)
        self._buf[offset:offset+n] = data

    def write_bits(self, start_bit, width, value):
        _write_bits(self._buf, start_bit, width, value)

    def read_bits(self, start_bit, width):
        return _read_bits(self._buf, start_bit, width)

    def get_bytes(self, start, length):
        return bytes(self._buf[start:start+length])

    def __len__(self):
        return 18 + self._data_length

    def __bytes__(self):
        return bytes(self._buf[:18 + self._data_length])

    def __iter__(self):
        return iter(self._buf[:18 + self._data_length])

    def __getitem__(self, key):
        return self._buf[:18 + self._data_length][key]

    def __enter__(self):
        return self

    def __exit__(self, *a):
        pass

CxlIoCfgRdPacket = _GenCxlIoCfgRdPacket

class _GenCxlIoCfgWrPacket:
    _header_size = 18

    def __init__(self, payload=None):
        self._buf = bytearray(512)
        self._data_length = 0
        self._system_header = SystemHeader()
        self._system_header._attach(self._buf, 0)
        self._tlp_prefix = TlpPrefix()
        self._tlp_prefix._attach(self._buf, 2)
        self._cxl_io_header = CxlIoHeader()
        self._cxl_io_header._attach(self._buf, 6)
        self._cfg_req_header = CxlIoCfgReqHeader()
        self._cfg_req_header._attach(self._buf, 10)
        if payload is not None:
            n = len(payload)
            if n > 512:
                raise ValueError("packet too large")
            self._buf[:n] = payload
            self._data_length = n - 18

    @property
    def system_header(self):
        return self._system_header

    @property
    def tlp_prefix(self):
        return self._tlp_prefix

    @property
    def cxl_io_header(self):
        return self._cxl_io_header

    @property
    def cfg_req_header(self):
        return self._cfg_req_header

    @classmethod
    def create(cls, system_header__payload_type, tlp_prefix__ld_id, cxl_io_header__fmt_type, cxl_io_header__length_upper, cxl_io_header__length_lower, cfg_req_header__req_id, cfg_req_header__tag, cfg_req_header__first_dw_be, cfg_req_header__last_dw_be, cfg_req_header__dest_id, cfg_req_header__ext_reg_num, cfg_req_header__reg_num, data: bytes = None):
        pkt = cls()
        pkt._buf = bytearray(512)
        pkt._system_header._attach(pkt._buf, 0)
        pkt._tlp_prefix._attach(pkt._buf, 2)
        pkt._cxl_io_header._attach(pkt._buf, 6)
        pkt._cfg_req_header._attach(pkt._buf, 10)
        pkt._system_header.payload_type = system_header__payload_type
        pkt._tlp_prefix.ld_id = tlp_prefix__ld_id
        pkt._cxl_io_header.fmt_type = cxl_io_header__fmt_type
        pkt._cxl_io_header.length_upper = cxl_io_header__length_upper
        pkt._cxl_io_header.length_lower = cxl_io_header__length_lower
        pkt._cfg_req_header.req_id = cfg_req_header__req_id
        pkt._cfg_req_header.tag = cfg_req_header__tag
        pkt._cfg_req_header.first_dw_be = cfg_req_header__first_dw_be
        pkt._cfg_req_header.last_dw_be = cfg_req_header__last_dw_be
        pkt._cfg_req_header.dest_id = cfg_req_header__dest_id
        pkt._cfg_req_header.ext_reg_num = cfg_req_header__ext_reg_num
        pkt._cfg_req_header.reg_num = cfg_req_header__reg_num
        if data is not None:
            ptr = 18
            n = len(data)
            if 18 + n > 512:
                raise ValueError("data too large")
            pkt._buf[ptr:ptr+n] = data
            pkt._data_length = n
        pkt._system_header.payload_length = 18 + pkt._data_length
        return pkt

    def assign(self, system_header__payload_type, tlp_prefix__ld_id, cxl_io_header__fmt_type, cxl_io_header__length_upper, cxl_io_header__length_lower, cfg_req_header__req_id, cfg_req_header__tag, cfg_req_header__first_dw_be, cfg_req_header__last_dw_be, cfg_req_header__dest_id, cfg_req_header__ext_reg_num, cfg_req_header__reg_num, data: bytes = None):
        self._buf = bytearray(512)
        self._system_header._attach(self._buf, 0)
        self._tlp_prefix._attach(self._buf, 2)
        self._cxl_io_header._attach(self._buf, 6)
        self._cfg_req_header._attach(self._buf, 10)
        self._system_header.payload_type = system_header__payload_type
        self._tlp_prefix.ld_id = tlp_prefix__ld_id
        self._cxl_io_header.fmt_type = cxl_io_header__fmt_type
        self._cxl_io_header.length_upper = cxl_io_header__length_upper
        self._cxl_io_header.length_lower = cxl_io_header__length_lower
        self._cfg_req_header.req_id = cfg_req_header__req_id
        self._cfg_req_header.tag = cfg_req_header__tag
        self._cfg_req_header.first_dw_be = cfg_req_header__first_dw_be
        self._cfg_req_header.last_dw_be = cfg_req_header__last_dw_be
        self._cfg_req_header.dest_id = cfg_req_header__dest_id
        self._cfg_req_header.ext_reg_num = cfg_req_header__ext_reg_num
        self._cfg_req_header.reg_num = cfg_req_header__reg_num
        if data is not None:
            ptr = 18
            n = len(data)
            self._buf[ptr:ptr+n] = data
            self._data_length = n
        self._system_header.payload_length = 18 + self._data_length

    def set_data(self, data):
        n = len(data)
        if 18 + n > 512:
            raise ValueError("packet too large")
        self._buf[18:18+n] = data
        self._data_length = n

    def get_data(self):
        return bytes(self._buf[18:18+self._data_length])

    def get_payload_offset(self):
        return 18

    def get_size(self):
        return 18 + self._data_length

    def get_byte_offset(self, other):
        if isinstance(other, SystemHeader):
            return 0
        elif isinstance(other, TlpPrefix):
            return 2
        elif isinstance(other, CxlIoHeader):
            return 6
        elif isinstance(other, CxlIoCfgReqHeader):
            return 10
        raise TypeError(f'unsupported header type: {type(other)}')

    def set_bytes(self, offset, data):
        n = len(data)
        self._buf[offset:offset+n] = data

    def write_bits(self, start_bit, width, value):
        _write_bits(self._buf, start_bit, width, value)

    def read_bits(self, start_bit, width):
        return _read_bits(self._buf, start_bit, width)

    def get_bytes(self, start, length):
        return bytes(self._buf[start:start+length])

    def __len__(self):
        return 18 + self._data_length

    def __bytes__(self):
        return bytes(self._buf[:18 + self._data_length])

    def __iter__(self):
        return iter(self._buf[:18 + self._data_length])

    def __getitem__(self, key):
        return self._buf[:18 + self._data_length][key]

    def __enter__(self):
        return self

    def __exit__(self, *a):
        pass

CxlIoCfgWrPacket = _GenCxlIoCfgWrPacket

class _GenCxlIoCompletionPacket:
    _header_size = 18

    def __init__(self, payload=None):
        self._buf = bytearray(512)
        self._data_length = 0
        self._system_header = SystemHeader()
        self._system_header._attach(self._buf, 0)
        self._tlp_prefix = TlpPrefix()
        self._tlp_prefix._attach(self._buf, 2)
        self._cxl_io_header = CxlIoHeader()
        self._cxl_io_header._attach(self._buf, 6)
        self._cpl_header = CxlIoCompletionHeader()
        self._cpl_header._attach(self._buf, 10)
        if payload is not None:
            n = len(payload)
            if n > 512:
                raise ValueError("packet too large")
            self._buf[:n] = payload
            self._data_length = n - 18

    @property
    def system_header(self):
        return self._system_header

    @property
    def tlp_prefix(self):
        return self._tlp_prefix

    @property
    def cxl_io_header(self):
        return self._cxl_io_header

    @property
    def cpl_header(self):
        return self._cpl_header

    @classmethod
    def create(cls, system_header__payload_type, tlp_prefix__ld_id, cxl_io_header__fmt_type, cxl_io_header__length_upper, cxl_io_header__length_lower, cpl_header__cpl_id, cpl_header__status, cpl_header__byte_count_upper, cpl_header__byte_count_lower, cpl_header__req_id, cpl_header__tag, data: bytes = None):
        pkt = cls()
        pkt._buf = bytearray(512)
        pkt._system_header._attach(pkt._buf, 0)
        pkt._tlp_prefix._attach(pkt._buf, 2)
        pkt._cxl_io_header._attach(pkt._buf, 6)
        pkt._cpl_header._attach(pkt._buf, 10)
        pkt._system_header.payload_type = system_header__payload_type
        pkt._tlp_prefix.ld_id = tlp_prefix__ld_id
        pkt._cxl_io_header.fmt_type = cxl_io_header__fmt_type
        pkt._cxl_io_header.length_upper = cxl_io_header__length_upper
        pkt._cxl_io_header.length_lower = cxl_io_header__length_lower
        pkt._cpl_header.cpl_id = cpl_header__cpl_id
        pkt._cpl_header.status = cpl_header__status
        pkt._cpl_header.byte_count_upper = cpl_header__byte_count_upper
        pkt._cpl_header.byte_count_lower = cpl_header__byte_count_lower
        pkt._cpl_header.req_id = cpl_header__req_id
        pkt._cpl_header.tag = cpl_header__tag
        if data is not None:
            ptr = 18
            n = len(data)
            if 18 + n > 512:
                raise ValueError("data too large")
            pkt._buf[ptr:ptr+n] = data
            pkt._data_length = n
        pkt._system_header.payload_length = 18 + pkt._data_length
        return pkt

    def assign(self, system_header__payload_type, tlp_prefix__ld_id, cxl_io_header__fmt_type, cxl_io_header__length_upper, cxl_io_header__length_lower, cpl_header__cpl_id, cpl_header__status, cpl_header__byte_count_upper, cpl_header__byte_count_lower, cpl_header__req_id, cpl_header__tag, data: bytes = None):
        self._buf = bytearray(512)
        self._system_header._attach(self._buf, 0)
        self._tlp_prefix._attach(self._buf, 2)
        self._cxl_io_header._attach(self._buf, 6)
        self._cpl_header._attach(self._buf, 10)
        self._system_header.payload_type = system_header__payload_type
        self._tlp_prefix.ld_id = tlp_prefix__ld_id
        self._cxl_io_header.fmt_type = cxl_io_header__fmt_type
        self._cxl_io_header.length_upper = cxl_io_header__length_upper
        self._cxl_io_header.length_lower = cxl_io_header__length_lower
        self._cpl_header.cpl_id = cpl_header__cpl_id
        self._cpl_header.status = cpl_header__status
        self._cpl_header.byte_count_upper = cpl_header__byte_count_upper
        self._cpl_header.byte_count_lower = cpl_header__byte_count_lower
        self._cpl_header.req_id = cpl_header__req_id
        self._cpl_header.tag = cpl_header__tag
        if data is not None:
            ptr = 18
            n = len(data)
            self._buf[ptr:ptr+n] = data
            self._data_length = n
        self._system_header.payload_length = 18 + self._data_length

    def set_data(self, data):
        n = len(data)
        if 18 + n > 512:
            raise ValueError("packet too large")
        self._buf[18:18+n] = data
        self._data_length = n

    def get_data(self):
        return bytes(self._buf[18:18+self._data_length])

    def get_payload_offset(self):
        return 18

    def get_size(self):
        return 18 + self._data_length

    def get_byte_offset(self, other):
        if isinstance(other, SystemHeader):
            return 0
        elif isinstance(other, TlpPrefix):
            return 2
        elif isinstance(other, CxlIoHeader):
            return 6
        elif isinstance(other, CxlIoCompletionHeader):
            return 10
        raise TypeError(f'unsupported header type: {type(other)}')

    def set_bytes(self, offset, data):
        n = len(data)
        self._buf[offset:offset+n] = data

    def write_bits(self, start_bit, width, value):
        _write_bits(self._buf, start_bit, width, value)

    def read_bits(self, start_bit, width):
        return _read_bits(self._buf, start_bit, width)

    def get_bytes(self, start, length):
        return bytes(self._buf[start:start+length])

    def __len__(self):
        return 18 + self._data_length

    def __bytes__(self):
        return bytes(self._buf[:18 + self._data_length])

    def __iter__(self):
        return iter(self._buf[:18 + self._data_length])

    def __getitem__(self, key):
        return self._buf[:18 + self._data_length][key]

    def __enter__(self):
        return self

    def __exit__(self, *a):
        pass

CxlIoCompletionPacket = _GenCxlIoCompletionPacket

class _GenCxlCacheBasePacket:
    _header_size = 4

    def __init__(self, payload=None):
        self._buf = bytearray(512)
        self._data_length = 0
        self._system_header = SystemHeader()
        self._system_header._attach(self._buf, 0)
        self._cxl_cache_header = CxlCacheHeader()
        self._cxl_cache_header._attach(self._buf, 2)
        if payload is not None:
            n = len(payload)
            if n > 512:
                raise ValueError("packet too large")
            self._buf[:n] = payload
            self._data_length = n - 4

    @property
    def system_header(self):
        return self._system_header

    @property
    def cxl_cache_header(self):
        return self._cxl_cache_header

    def set_data(self, data):
        n = len(data)
        if 4 + n > 512:
            raise ValueError("packet too large")
        self._buf[4:4+n] = data
        self._data_length = n

    def get_data(self):
        return bytes(self._buf[4:4+self._data_length])

    def get_payload_offset(self):
        return 4

    def get_size(self):
        return 4 + self._data_length

    def get_byte_offset(self, other):
        if isinstance(other, SystemHeader):
            return 0
        elif isinstance(other, CxlCacheHeader):
            return 2
        raise TypeError(f'unsupported header type: {type(other)}')

    def set_bytes(self, offset, data):
        n = len(data)
        self._buf[offset:offset+n] = data

    def write_bits(self, start_bit, width, value):
        _write_bits(self._buf, start_bit, width, value)

    def read_bits(self, start_bit, width):
        return _read_bits(self._buf, start_bit, width)

    def get_bytes(self, start, length):
        return bytes(self._buf[start:start+length])

    def __len__(self):
        return 4 + self._data_length

    def __bytes__(self):
        return bytes(self._buf[:4 + self._data_length])

    def __iter__(self):
        return iter(self._buf[:4 + self._data_length])

    def __getitem__(self, key):
        return self._buf[:4 + self._data_length][key]

    def __enter__(self):
        return self

    def __exit__(self, *a):
        pass

CxlCacheBasePacket = _GenCxlCacheBasePacket

class _GenCxlCacheD2HReqPacket:
    _header_size = 13

    def __init__(self, payload=None):
        self._buf = bytearray(512)
        self._data_length = 0
        self._system_header = SystemHeader()
        self._system_header._attach(self._buf, 0)
        self._cxl_cache_header = CxlCacheHeader()
        self._cxl_cache_header._attach(self._buf, 2)
        self._d2hreq_header = CxlCacheD2HReqHeader()
        self._d2hreq_header._attach(self._buf, 4)
        if payload is not None:
            n = len(payload)
            if n > 512:
                raise ValueError("packet too large")
            self._buf[:n] = payload
            self._data_length = n - 13

    @property
    def system_header(self):
        return self._system_header

    @property
    def cxl_cache_header(self):
        return self._cxl_cache_header

    @property
    def d2hreq_header(self):
        return self._d2hreq_header

    @classmethod
    def create(cls, system_header__payload_type, cxl_cache_header__msg_class, d2hreq_header__valid, d2hreq_header__cache_opcode, d2hreq_header__cqid, d2hreq_header__cache_id, d2hreq_header__addr, data: bytes = None):
        pkt = cls()
        pkt._buf = bytearray(512)
        pkt._system_header._attach(pkt._buf, 0)
        pkt._cxl_cache_header._attach(pkt._buf, 2)
        pkt._d2hreq_header._attach(pkt._buf, 4)
        pkt._system_header.payload_type = system_header__payload_type
        pkt._cxl_cache_header.msg_class = cxl_cache_header__msg_class
        pkt._d2hreq_header.valid = d2hreq_header__valid
        pkt._d2hreq_header.cache_opcode = d2hreq_header__cache_opcode
        pkt._d2hreq_header.cqid = d2hreq_header__cqid
        pkt._d2hreq_header.cache_id = d2hreq_header__cache_id
        pkt._d2hreq_header.addr = d2hreq_header__addr
        if data is not None:
            ptr = 13
            n = len(data)
            if 13 + n > 512:
                raise ValueError("data too large")
            pkt._buf[ptr:ptr+n] = data
            pkt._data_length = n
        pkt._system_header.payload_length = 13 + pkt._data_length
        return pkt

    def assign(self, system_header__payload_type, cxl_cache_header__msg_class, d2hreq_header__valid, d2hreq_header__cache_opcode, d2hreq_header__cqid, d2hreq_header__cache_id, d2hreq_header__addr, data: bytes = None):
        self._buf = bytearray(512)
        self._system_header._attach(self._buf, 0)
        self._cxl_cache_header._attach(self._buf, 2)
        self._d2hreq_header._attach(self._buf, 4)
        self._system_header.payload_type = system_header__payload_type
        self._cxl_cache_header.msg_class = cxl_cache_header__msg_class
        self._d2hreq_header.valid = d2hreq_header__valid
        self._d2hreq_header.cache_opcode = d2hreq_header__cache_opcode
        self._d2hreq_header.cqid = d2hreq_header__cqid
        self._d2hreq_header.cache_id = d2hreq_header__cache_id
        self._d2hreq_header.addr = d2hreq_header__addr
        if data is not None:
            ptr = 13
            n = len(data)
            self._buf[ptr:ptr+n] = data
            self._data_length = n
        self._system_header.payload_length = 13 + self._data_length

    def set_data(self, data):
        n = len(data)
        if 13 + n > 512:
            raise ValueError("packet too large")
        self._buf[13:13+n] = data
        self._data_length = n

    def get_data(self):
        return bytes(self._buf[13:13+self._data_length])

    def get_payload_offset(self):
        return 13

    def get_size(self):
        return 13 + self._data_length

    def get_byte_offset(self, other):
        if isinstance(other, SystemHeader):
            return 0
        elif isinstance(other, CxlCacheHeader):
            return 2
        elif isinstance(other, CxlCacheD2HReqHeader):
            return 4
        raise TypeError(f'unsupported header type: {type(other)}')

    def set_bytes(self, offset, data):
        n = len(data)
        self._buf[offset:offset+n] = data

    def write_bits(self, start_bit, width, value):
        _write_bits(self._buf, start_bit, width, value)

    def read_bits(self, start_bit, width):
        return _read_bits(self._buf, start_bit, width)

    def get_bytes(self, start, length):
        return bytes(self._buf[start:start+length])

    def __len__(self):
        return 13 + self._data_length

    def __bytes__(self):
        return bytes(self._buf[:13 + self._data_length])

    def __iter__(self):
        return iter(self._buf[:13 + self._data_length])

    def __getitem__(self, key):
        return self._buf[:13 + self._data_length][key]

    def __enter__(self):
        return self

    def __exit__(self, *a):
        pass

CxlCacheD2HReqPacket = _GenCxlCacheD2HReqPacket

class _GenCxlCacheD2HRspPacket:
    _header_size = 7

    def __init__(self, payload=None):
        self._buf = bytearray(512)
        self._data_length = 0
        self._system_header = SystemHeader()
        self._system_header._attach(self._buf, 0)
        self._cxl_cache_header = CxlCacheHeader()
        self._cxl_cache_header._attach(self._buf, 2)
        self._d2hrsp_header = CxlCacheD2HRspHeader()
        self._d2hrsp_header._attach(self._buf, 4)
        if payload is not None:
            n = len(payload)
            if n > 512:
                raise ValueError("packet too large")
            self._buf[:n] = payload
            self._data_length = n - 7

    @property
    def system_header(self):
        return self._system_header

    @property
    def cxl_cache_header(self):
        return self._cxl_cache_header

    @property
    def d2hrsp_header(self):
        return self._d2hrsp_header

    @classmethod
    def create(cls, system_header__payload_type, cxl_cache_header__msg_class, d2hrsp_header__valid, d2hrsp_header__uqid, d2hrsp_header__cache_opcode, data: bytes = None):
        pkt = cls()
        pkt._buf = bytearray(512)
        pkt._system_header._attach(pkt._buf, 0)
        pkt._cxl_cache_header._attach(pkt._buf, 2)
        pkt._d2hrsp_header._attach(pkt._buf, 4)
        pkt._system_header.payload_type = system_header__payload_type
        pkt._cxl_cache_header.msg_class = cxl_cache_header__msg_class
        pkt._d2hrsp_header.valid = d2hrsp_header__valid
        pkt._d2hrsp_header.uqid = d2hrsp_header__uqid
        pkt._d2hrsp_header.cache_opcode = d2hrsp_header__cache_opcode
        if data is not None:
            ptr = 7
            n = len(data)
            if 7 + n > 512:
                raise ValueError("data too large")
            pkt._buf[ptr:ptr+n] = data
            pkt._data_length = n
        pkt._system_header.payload_length = 7 + pkt._data_length
        return pkt

    def assign(self, system_header__payload_type, cxl_cache_header__msg_class, d2hrsp_header__valid, d2hrsp_header__uqid, d2hrsp_header__cache_opcode, data: bytes = None):
        self._buf = bytearray(512)
        self._system_header._attach(self._buf, 0)
        self._cxl_cache_header._attach(self._buf, 2)
        self._d2hrsp_header._attach(self._buf, 4)
        self._system_header.payload_type = system_header__payload_type
        self._cxl_cache_header.msg_class = cxl_cache_header__msg_class
        self._d2hrsp_header.valid = d2hrsp_header__valid
        self._d2hrsp_header.uqid = d2hrsp_header__uqid
        self._d2hrsp_header.cache_opcode = d2hrsp_header__cache_opcode
        if data is not None:
            ptr = 7
            n = len(data)
            self._buf[ptr:ptr+n] = data
            self._data_length = n
        self._system_header.payload_length = 7 + self._data_length

    def set_data(self, data):
        n = len(data)
        if 7 + n > 512:
            raise ValueError("packet too large")
        self._buf[7:7+n] = data
        self._data_length = n

    def get_data(self):
        return bytes(self._buf[7:7+self._data_length])

    def get_payload_offset(self):
        return 7

    def get_size(self):
        return 7 + self._data_length

    def get_byte_offset(self, other):
        if isinstance(other, SystemHeader):
            return 0
        elif isinstance(other, CxlCacheHeader):
            return 2
        elif isinstance(other, CxlCacheD2HRspHeader):
            return 4
        raise TypeError(f'unsupported header type: {type(other)}')

    def set_bytes(self, offset, data):
        n = len(data)
        self._buf[offset:offset+n] = data

    def write_bits(self, start_bit, width, value):
        _write_bits(self._buf, start_bit, width, value)

    def read_bits(self, start_bit, width):
        return _read_bits(self._buf, start_bit, width)

    def get_bytes(self, start, length):
        return bytes(self._buf[start:start+length])

    def __len__(self):
        return 7 + self._data_length

    def __bytes__(self):
        return bytes(self._buf[:7 + self._data_length])

    def __iter__(self):
        return iter(self._buf[:7 + self._data_length])

    def __getitem__(self, key):
        return self._buf[:7 + self._data_length][key]

    def __enter__(self):
        return self

    def __exit__(self, *a):
        pass

CxlCacheD2HRspPacket = _GenCxlCacheD2HRspPacket

class _GenCxlCacheD2HDataPacket:
    _header_size = 7

    def __init__(self, payload=None):
        self._buf = bytearray(512)
        self._data_length = 0
        self._system_header = SystemHeader()
        self._system_header._attach(self._buf, 0)
        self._cxl_cache_header = CxlCacheHeader()
        self._cxl_cache_header._attach(self._buf, 2)
        self._d2hdata_header = CxlCacheD2HDataHeader()
        self._d2hdata_header._attach(self._buf, 4)
        if payload is not None:
            n = len(payload)
            if n > 512:
                raise ValueError("packet too large")
            self._buf[:n] = payload
            self._data_length = n - 7

    @property
    def system_header(self):
        return self._system_header

    @property
    def cxl_cache_header(self):
        return self._cxl_cache_header

    @property
    def d2hdata_header(self):
        return self._d2hdata_header

    @classmethod
    def create(cls, system_header__payload_type, cxl_cache_header__msg_class, d2hdata_header__valid, d2hdata_header__uqid, d2hdata_header__poison, data: bytes = None):
        pkt = cls()
        pkt._buf = bytearray(512)
        pkt._system_header._attach(pkt._buf, 0)
        pkt._cxl_cache_header._attach(pkt._buf, 2)
        pkt._d2hdata_header._attach(pkt._buf, 4)
        pkt._system_header.payload_type = system_header__payload_type
        pkt._cxl_cache_header.msg_class = cxl_cache_header__msg_class
        pkt._d2hdata_header.valid = d2hdata_header__valid
        pkt._d2hdata_header.uqid = d2hdata_header__uqid
        pkt._d2hdata_header.poison = d2hdata_header__poison
        if data is not None:
            ptr = 7
            n = len(data)
            if 7 + n > 512:
                raise ValueError("data too large")
            pkt._buf[ptr:ptr+n] = data
            pkt._data_length = n
        pkt._system_header.payload_length = 7 + pkt._data_length
        return pkt

    def assign(self, system_header__payload_type, cxl_cache_header__msg_class, d2hdata_header__valid, d2hdata_header__uqid, d2hdata_header__poison, data: bytes = None):
        self._buf = bytearray(512)
        self._system_header._attach(self._buf, 0)
        self._cxl_cache_header._attach(self._buf, 2)
        self._d2hdata_header._attach(self._buf, 4)
        self._system_header.payload_type = system_header__payload_type
        self._cxl_cache_header.msg_class = cxl_cache_header__msg_class
        self._d2hdata_header.valid = d2hdata_header__valid
        self._d2hdata_header.uqid = d2hdata_header__uqid
        self._d2hdata_header.poison = d2hdata_header__poison
        if data is not None:
            ptr = 7
            n = len(data)
            self._buf[ptr:ptr+n] = data
            self._data_length = n
        self._system_header.payload_length = 7 + self._data_length

    def set_data(self, data):
        n = len(data)
        if 7 + n > 512:
            raise ValueError("packet too large")
        self._buf[7:7+n] = data
        self._data_length = n

    def get_data(self):
        return bytes(self._buf[7:7+self._data_length])

    def get_payload_offset(self):
        return 7

    def get_size(self):
        return 7 + self._data_length

    def get_byte_offset(self, other):
        if isinstance(other, SystemHeader):
            return 0
        elif isinstance(other, CxlCacheHeader):
            return 2
        elif isinstance(other, CxlCacheD2HDataHeader):
            return 4
        raise TypeError(f'unsupported header type: {type(other)}')

    def set_bytes(self, offset, data):
        n = len(data)
        self._buf[offset:offset+n] = data

    def write_bits(self, start_bit, width, value):
        _write_bits(self._buf, start_bit, width, value)

    def read_bits(self, start_bit, width):
        return _read_bits(self._buf, start_bit, width)

    def get_bytes(self, start, length):
        return bytes(self._buf[start:start+length])

    def __len__(self):
        return 7 + self._data_length

    def __bytes__(self):
        return bytes(self._buf[:7 + self._data_length])

    def __iter__(self):
        return iter(self._buf[:7 + self._data_length])

    def __getitem__(self, key):
        return self._buf[:7 + self._data_length][key]

    def __enter__(self):
        return self

    def __exit__(self, *a):
        pass

CxlCacheD2HDataPacket = _GenCxlCacheD2HDataPacket

class _GenCxlCacheH2DReqPacket:
    _header_size = 13

    def __init__(self, payload=None):
        self._buf = bytearray(512)
        self._data_length = 0
        self._system_header = SystemHeader()
        self._system_header._attach(self._buf, 0)
        self._cxl_cache_header = CxlCacheHeader()
        self._cxl_cache_header._attach(self._buf, 2)
        self._h2dreq_header = CxlCacheH2DReqHeader()
        self._h2dreq_header._attach(self._buf, 4)
        if payload is not None:
            n = len(payload)
            if n > 512:
                raise ValueError("packet too large")
            self._buf[:n] = payload
            self._data_length = n - 13

    @property
    def system_header(self):
        return self._system_header

    @property
    def cxl_cache_header(self):
        return self._cxl_cache_header

    @property
    def h2dreq_header(self):
        return self._h2dreq_header

    @classmethod
    def create(cls, system_header__payload_type, cxl_cache_header__msg_class, h2dreq_header__valid, h2dreq_header__cache_opcode, h2dreq_header__cache_id, h2dreq_header__addr, data: bytes = None):
        pkt = cls()
        pkt._buf = bytearray(512)
        pkt._system_header._attach(pkt._buf, 0)
        pkt._cxl_cache_header._attach(pkt._buf, 2)
        pkt._h2dreq_header._attach(pkt._buf, 4)
        pkt._system_header.payload_type = system_header__payload_type
        pkt._cxl_cache_header.msg_class = cxl_cache_header__msg_class
        pkt._h2dreq_header.valid = h2dreq_header__valid
        pkt._h2dreq_header.cache_opcode = h2dreq_header__cache_opcode
        pkt._h2dreq_header.cache_id = h2dreq_header__cache_id
        pkt._h2dreq_header.addr = h2dreq_header__addr
        if data is not None:
            ptr = 13
            n = len(data)
            if 13 + n > 512:
                raise ValueError("data too large")
            pkt._buf[ptr:ptr+n] = data
            pkt._data_length = n
        pkt._system_header.payload_length = 13 + pkt._data_length
        return pkt

    def assign(self, system_header__payload_type, cxl_cache_header__msg_class, h2dreq_header__valid, h2dreq_header__cache_opcode, h2dreq_header__cache_id, h2dreq_header__addr, data: bytes = None):
        self._buf = bytearray(512)
        self._system_header._attach(self._buf, 0)
        self._cxl_cache_header._attach(self._buf, 2)
        self._h2dreq_header._attach(self._buf, 4)
        self._system_header.payload_type = system_header__payload_type
        self._cxl_cache_header.msg_class = cxl_cache_header__msg_class
        self._h2dreq_header.valid = h2dreq_header__valid
        self._h2dreq_header.cache_opcode = h2dreq_header__cache_opcode
        self._h2dreq_header.cache_id = h2dreq_header__cache_id
        self._h2dreq_header.addr = h2dreq_header__addr
        if data is not None:
            ptr = 13
            n = len(data)
            self._buf[ptr:ptr+n] = data
            self._data_length = n
        self._system_header.payload_length = 13 + self._data_length

    def set_data(self, data):
        n = len(data)
        if 13 + n > 512:
            raise ValueError("packet too large")
        self._buf[13:13+n] = data
        self._data_length = n

    def get_data(self):
        return bytes(self._buf[13:13+self._data_length])

    def get_payload_offset(self):
        return 13

    def get_size(self):
        return 13 + self._data_length

    def get_byte_offset(self, other):
        if isinstance(other, SystemHeader):
            return 0
        elif isinstance(other, CxlCacheHeader):
            return 2
        elif isinstance(other, CxlCacheH2DReqHeader):
            return 4
        raise TypeError(f'unsupported header type: {type(other)}')

    def set_bytes(self, offset, data):
        n = len(data)
        self._buf[offset:offset+n] = data

    def write_bits(self, start_bit, width, value):
        _write_bits(self._buf, start_bit, width, value)

    def read_bits(self, start_bit, width):
        return _read_bits(self._buf, start_bit, width)

    def get_bytes(self, start, length):
        return bytes(self._buf[start:start+length])

    def __len__(self):
        return 13 + self._data_length

    def __bytes__(self):
        return bytes(self._buf[:13 + self._data_length])

    def __iter__(self):
        return iter(self._buf[:13 + self._data_length])

    def __getitem__(self, key):
        return self._buf[:13 + self._data_length][key]

    def __enter__(self):
        return self

    def __exit__(self, *a):
        pass

CxlCacheH2DReqPacket = _GenCxlCacheH2DReqPacket

class _GenCxlCacheH2DRspPacket:
    _header_size = 9

    def __init__(self, payload=None):
        self._buf = bytearray(512)
        self._data_length = 0
        self._system_header = SystemHeader()
        self._system_header._attach(self._buf, 0)
        self._cxl_cache_header = CxlCacheHeader()
        self._cxl_cache_header._attach(self._buf, 2)
        self._h2drsp_header = CxlCacheH2DRspHeader()
        self._h2drsp_header._attach(self._buf, 4)
        if payload is not None:
            n = len(payload)
            if n > 512:
                raise ValueError("packet too large")
            self._buf[:n] = payload
            self._data_length = n - 9

    @property
    def system_header(self):
        return self._system_header

    @property
    def cxl_cache_header(self):
        return self._cxl_cache_header

    @property
    def h2drsp_header(self):
        return self._h2drsp_header

    @classmethod
    def create(cls, system_header__payload_type, cxl_cache_header__msg_class, h2drsp_header__valid, h2drsp_header__cache_opcode, h2drsp_header__cache_id, h2drsp_header__rsp_data, h2drsp_header__cqid, data: bytes = None):
        pkt = cls()
        pkt._buf = bytearray(512)
        pkt._system_header._attach(pkt._buf, 0)
        pkt._cxl_cache_header._attach(pkt._buf, 2)
        pkt._h2drsp_header._attach(pkt._buf, 4)
        pkt._system_header.payload_type = system_header__payload_type
        pkt._cxl_cache_header.msg_class = cxl_cache_header__msg_class
        pkt._h2drsp_header.valid = h2drsp_header__valid
        pkt._h2drsp_header.cache_opcode = h2drsp_header__cache_opcode
        pkt._h2drsp_header.cache_id = h2drsp_header__cache_id
        pkt._h2drsp_header.rsp_data = h2drsp_header__rsp_data
        pkt._h2drsp_header.cqid = h2drsp_header__cqid
        if data is not None:
            ptr = 9
            n = len(data)
            if 9 + n > 512:
                raise ValueError("data too large")
            pkt._buf[ptr:ptr+n] = data
            pkt._data_length = n
        pkt._system_header.payload_length = 9 + pkt._data_length
        return pkt

    def assign(self, system_header__payload_type, cxl_cache_header__msg_class, h2drsp_header__valid, h2drsp_header__cache_opcode, h2drsp_header__cache_id, h2drsp_header__rsp_data, h2drsp_header__cqid, data: bytes = None):
        self._buf = bytearray(512)
        self._system_header._attach(self._buf, 0)
        self._cxl_cache_header._attach(self._buf, 2)
        self._h2drsp_header._attach(self._buf, 4)
        self._system_header.payload_type = system_header__payload_type
        self._cxl_cache_header.msg_class = cxl_cache_header__msg_class
        self._h2drsp_header.valid = h2drsp_header__valid
        self._h2drsp_header.cache_opcode = h2drsp_header__cache_opcode
        self._h2drsp_header.cache_id = h2drsp_header__cache_id
        self._h2drsp_header.rsp_data = h2drsp_header__rsp_data
        self._h2drsp_header.cqid = h2drsp_header__cqid
        if data is not None:
            ptr = 9
            n = len(data)
            self._buf[ptr:ptr+n] = data
            self._data_length = n
        self._system_header.payload_length = 9 + self._data_length

    def set_data(self, data):
        n = len(data)
        if 9 + n > 512:
            raise ValueError("packet too large")
        self._buf[9:9+n] = data
        self._data_length = n

    def get_data(self):
        return bytes(self._buf[9:9+self._data_length])

    def get_payload_offset(self):
        return 9

    def get_size(self):
        return 9 + self._data_length

    def get_byte_offset(self, other):
        if isinstance(other, SystemHeader):
            return 0
        elif isinstance(other, CxlCacheHeader):
            return 2
        elif isinstance(other, CxlCacheH2DRspHeader):
            return 4
        raise TypeError(f'unsupported header type: {type(other)}')

    def set_bytes(self, offset, data):
        n = len(data)
        self._buf[offset:offset+n] = data

    def write_bits(self, start_bit, width, value):
        _write_bits(self._buf, start_bit, width, value)

    def read_bits(self, start_bit, width):
        return _read_bits(self._buf, start_bit, width)

    def get_bytes(self, start, length):
        return bytes(self._buf[start:start+length])

    def __len__(self):
        return 9 + self._data_length

    def __bytes__(self):
        return bytes(self._buf[:9 + self._data_length])

    def __iter__(self):
        return iter(self._buf[:9 + self._data_length])

    def __getitem__(self, key):
        return self._buf[:9 + self._data_length][key]

    def __enter__(self):
        return self

    def __exit__(self, *a):
        pass

CxlCacheH2DRspPacket = _GenCxlCacheH2DRspPacket

class _GenCxlCacheH2DDataPacket:
    _header_size = 7

    def __init__(self, payload=None):
        self._buf = bytearray(512)
        self._data_length = 0
        self._system_header = SystemHeader()
        self._system_header._attach(self._buf, 0)
        self._cxl_cache_header = CxlCacheHeader()
        self._cxl_cache_header._attach(self._buf, 2)
        self._h2ddata_header = CxlCacheH2DDataHeader()
        self._h2ddata_header._attach(self._buf, 4)
        if payload is not None:
            n = len(payload)
            if n > 512:
                raise ValueError("packet too large")
            self._buf[:n] = payload
            self._data_length = n - 7

    @property
    def system_header(self):
        return self._system_header

    @property
    def cxl_cache_header(self):
        return self._cxl_cache_header

    @property
    def h2ddata_header(self):
        return self._h2ddata_header

    @classmethod
    def create(cls, system_header__payload_type, cxl_cache_header__msg_class, h2ddata_header__valid, h2ddata_header__cache_id, h2ddata_header__cqid, data: bytes = None):
        pkt = cls()
        pkt._buf = bytearray(512)
        pkt._system_header._attach(pkt._buf, 0)
        pkt._cxl_cache_header._attach(pkt._buf, 2)
        pkt._h2ddata_header._attach(pkt._buf, 4)
        pkt._system_header.payload_type = system_header__payload_type
        pkt._cxl_cache_header.msg_class = cxl_cache_header__msg_class
        pkt._h2ddata_header.valid = h2ddata_header__valid
        pkt._h2ddata_header.cache_id = h2ddata_header__cache_id
        pkt._h2ddata_header.cqid = h2ddata_header__cqid
        if data is not None:
            ptr = 7
            n = len(data)
            if 7 + n > 512:
                raise ValueError("data too large")
            pkt._buf[ptr:ptr+n] = data
            pkt._data_length = n
        pkt._system_header.payload_length = 7 + pkt._data_length
        return pkt

    def assign(self, system_header__payload_type, cxl_cache_header__msg_class, h2ddata_header__valid, h2ddata_header__cache_id, h2ddata_header__cqid, data: bytes = None):
        self._buf = bytearray(512)
        self._system_header._attach(self._buf, 0)
        self._cxl_cache_header._attach(self._buf, 2)
        self._h2ddata_header._attach(self._buf, 4)
        self._system_header.payload_type = system_header__payload_type
        self._cxl_cache_header.msg_class = cxl_cache_header__msg_class
        self._h2ddata_header.valid = h2ddata_header__valid
        self._h2ddata_header.cache_id = h2ddata_header__cache_id
        self._h2ddata_header.cqid = h2ddata_header__cqid
        if data is not None:
            ptr = 7
            n = len(data)
            self._buf[ptr:ptr+n] = data
            self._data_length = n
        self._system_header.payload_length = 7 + self._data_length

    def set_data(self, data):
        n = len(data)
        if 7 + n > 512:
            raise ValueError("packet too large")
        self._buf[7:7+n] = data
        self._data_length = n

    def get_data(self):
        return bytes(self._buf[7:7+self._data_length])

    def get_payload_offset(self):
        return 7

    def get_size(self):
        return 7 + self._data_length

    def get_byte_offset(self, other):
        if isinstance(other, SystemHeader):
            return 0
        elif isinstance(other, CxlCacheHeader):
            return 2
        elif isinstance(other, CxlCacheH2DDataHeader):
            return 4
        raise TypeError(f'unsupported header type: {type(other)}')

    def set_bytes(self, offset, data):
        n = len(data)
        self._buf[offset:offset+n] = data

    def write_bits(self, start_bit, width, value):
        _write_bits(self._buf, start_bit, width, value)

    def read_bits(self, start_bit, width):
        return _read_bits(self._buf, start_bit, width)

    def get_bytes(self, start, length):
        return bytes(self._buf[start:start+length])

    def __len__(self):
        return 7 + self._data_length

    def __bytes__(self):
        return bytes(self._buf[:7 + self._data_length])

    def __iter__(self):
        return iter(self._buf[:7 + self._data_length])

    def __getitem__(self, key):
        return self._buf[:7 + self._data_length][key]

    def __enter__(self):
        return self

    def __exit__(self, *a):
        pass

CxlCacheH2DDataPacket = _GenCxlCacheH2DDataPacket

class _GenCxlMemBasePacket:
    _header_size = 4

    def __init__(self, payload=None):
        self._buf = bytearray(512)
        self._data_length = 0
        self._system_header = SystemHeader()
        self._system_header._attach(self._buf, 0)
        self._cxl_mem_header = CxlMemHeader()
        self._cxl_mem_header._attach(self._buf, 2)
        if payload is not None:
            n = len(payload)
            if n > 512:
                raise ValueError("packet too large")
            self._buf[:n] = payload
            self._data_length = n - 4

    @property
    def system_header(self):
        return self._system_header

    @property
    def cxl_mem_header(self):
        return self._cxl_mem_header

    def set_data(self, data):
        n = len(data)
        if 4 + n > 512:
            raise ValueError("packet too large")
        self._buf[4:4+n] = data
        self._data_length = n

    def get_data(self):
        return bytes(self._buf[4:4+self._data_length])

    def get_payload_offset(self):
        return 4

    def get_size(self):
        return 4 + self._data_length

    def get_byte_offset(self, other):
        if isinstance(other, SystemHeader):
            return 0
        elif isinstance(other, CxlMemHeader):
            return 2
        raise TypeError(f'unsupported header type: {type(other)}')

    def set_bytes(self, offset, data):
        n = len(data)
        self._buf[offset:offset+n] = data

    def write_bits(self, start_bit, width, value):
        _write_bits(self._buf, start_bit, width, value)

    def read_bits(self, start_bit, width):
        return _read_bits(self._buf, start_bit, width)

    def get_bytes(self, start, length):
        return bytes(self._buf[start:start+length])

    def __len__(self):
        return 4 + self._data_length

    def __bytes__(self):
        return bytes(self._buf[:4 + self._data_length])

    def __iter__(self):
        return iter(self._buf[:4 + self._data_length])

    def __getitem__(self, key):
        return self._buf[:4 + self._data_length][key]

    def __enter__(self):
        return self

    def __exit__(self, *a):
        pass

CxlMemBasePacket = _GenCxlMemBasePacket

class _GenCxlMemM2SReqPacket:
    _header_size = 17

    def __init__(self, payload=None):
        self._buf = bytearray(512)
        self._data_length = 0
        self._system_header = SystemHeader()
        self._system_header._attach(self._buf, 0)
        self._cxl_mem_header = CxlMemHeader()
        self._cxl_mem_header._attach(self._buf, 2)
        self._m2sreq_header = CxlMemM2SReqHeader()
        self._m2sreq_header._attach(self._buf, 4)
        if payload is not None:
            n = len(payload)
            if n > 512:
                raise ValueError("packet too large")
            self._buf[:n] = payload
            self._data_length = n - 17

    @property
    def system_header(self):
        return self._system_header

    @property
    def cxl_mem_header(self):
        return self._cxl_mem_header

    @property
    def m2sreq_header(self):
        return self._m2sreq_header

    @classmethod
    def create(cls, system_header__payload_type, cxl_mem_header__msg_class, m2sreq_header__valid, m2sreq_header__mem_opcode, m2sreq_header__meta_field, m2sreq_header__meta_value, m2sreq_header__snp_type, m2sreq_header__ld_id, m2sreq_header__addr, data: bytes = None):
        pkt = cls()
        pkt._buf = bytearray(512)
        pkt._system_header._attach(pkt._buf, 0)
        pkt._cxl_mem_header._attach(pkt._buf, 2)
        pkt._m2sreq_header._attach(pkt._buf, 4)
        pkt._system_header.payload_type = system_header__payload_type
        pkt._cxl_mem_header.msg_class = cxl_mem_header__msg_class
        pkt._m2sreq_header.valid = m2sreq_header__valid
        pkt._m2sreq_header.mem_opcode = m2sreq_header__mem_opcode
        pkt._m2sreq_header.meta_field = m2sreq_header__meta_field
        pkt._m2sreq_header.meta_value = m2sreq_header__meta_value
        pkt._m2sreq_header.snp_type = m2sreq_header__snp_type
        pkt._m2sreq_header.ld_id = m2sreq_header__ld_id
        pkt._m2sreq_header.addr = m2sreq_header__addr
        if data is not None:
            ptr = 17
            n = len(data)
            if 17 + n > 512:
                raise ValueError("data too large")
            pkt._buf[ptr:ptr+n] = data
            pkt._data_length = n
        pkt._system_header.payload_length = 17 + pkt._data_length
        return pkt

    def assign(self, system_header__payload_type, cxl_mem_header__msg_class, m2sreq_header__valid, m2sreq_header__mem_opcode, m2sreq_header__meta_field, m2sreq_header__meta_value, m2sreq_header__snp_type, m2sreq_header__ld_id, m2sreq_header__addr, data: bytes = None):
        self._buf = bytearray(512)
        self._system_header._attach(self._buf, 0)
        self._cxl_mem_header._attach(self._buf, 2)
        self._m2sreq_header._attach(self._buf, 4)
        self._system_header.payload_type = system_header__payload_type
        self._cxl_mem_header.msg_class = cxl_mem_header__msg_class
        self._m2sreq_header.valid = m2sreq_header__valid
        self._m2sreq_header.mem_opcode = m2sreq_header__mem_opcode
        self._m2sreq_header.meta_field = m2sreq_header__meta_field
        self._m2sreq_header.meta_value = m2sreq_header__meta_value
        self._m2sreq_header.snp_type = m2sreq_header__snp_type
        self._m2sreq_header.ld_id = m2sreq_header__ld_id
        self._m2sreq_header.addr = m2sreq_header__addr
        if data is not None:
            ptr = 17
            n = len(data)
            self._buf[ptr:ptr+n] = data
            self._data_length = n
        self._system_header.payload_length = 17 + self._data_length

    def set_data(self, data):
        n = len(data)
        if 17 + n > 512:
            raise ValueError("packet too large")
        self._buf[17:17+n] = data
        self._data_length = n

    def get_data(self):
        return bytes(self._buf[17:17+self._data_length])

    def get_payload_offset(self):
        return 17

    def get_size(self):
        return 17 + self._data_length

    def get_byte_offset(self, other):
        if isinstance(other, SystemHeader):
            return 0
        elif isinstance(other, CxlMemHeader):
            return 2
        elif isinstance(other, CxlMemM2SReqHeader):
            return 4
        raise TypeError(f'unsupported header type: {type(other)}')

    def set_bytes(self, offset, data):
        n = len(data)
        self._buf[offset:offset+n] = data

    def write_bits(self, start_bit, width, value):
        _write_bits(self._buf, start_bit, width, value)

    def read_bits(self, start_bit, width):
        return _read_bits(self._buf, start_bit, width)

    def get_bytes(self, start, length):
        return bytes(self._buf[start:start+length])

    def __len__(self):
        return 17 + self._data_length

    def __bytes__(self):
        return bytes(self._buf[:17 + self._data_length])

    def __iter__(self):
        return iter(self._buf[:17 + self._data_length])

    def __getitem__(self, key):
        return self._buf[:17 + self._data_length][key]

    def __enter__(self):
        return self

    def __exit__(self, *a):
        pass

CxlMemM2SReqPacket = _GenCxlMemM2SReqPacket

class _GenCxlMemM2SRwDPacket:
    _header_size = 17

    def __init__(self, payload=None):
        self._buf = bytearray(512)
        self._data_length = 0
        self._system_header = SystemHeader()
        self._system_header._attach(self._buf, 0)
        self._cxl_mem_header = CxlMemHeader()
        self._cxl_mem_header._attach(self._buf, 2)
        self._m2srwd_header = CxlMemM2SRwDHeader()
        self._m2srwd_header._attach(self._buf, 4)
        if payload is not None:
            n = len(payload)
            if n > 512:
                raise ValueError("packet too large")
            self._buf[:n] = payload
            self._data_length = n - 17

    @property
    def system_header(self):
        return self._system_header

    @property
    def cxl_mem_header(self):
        return self._cxl_mem_header

    @property
    def m2srwd_header(self):
        return self._m2srwd_header

    @classmethod
    def create(cls, system_header__payload_type, cxl_mem_header__msg_class, m2srwd_header__valid, m2srwd_header__mem_opcode, m2srwd_header__meta_field, m2srwd_header__meta_value, m2srwd_header__snp_type, m2srwd_header__ld_id, m2srwd_header__addr, data: bytes = None):
        pkt = cls()
        pkt._buf = bytearray(512)
        pkt._system_header._attach(pkt._buf, 0)
        pkt._cxl_mem_header._attach(pkt._buf, 2)
        pkt._m2srwd_header._attach(pkt._buf, 4)
        pkt._system_header.payload_type = system_header__payload_type
        pkt._cxl_mem_header.msg_class = cxl_mem_header__msg_class
        pkt._m2srwd_header.valid = m2srwd_header__valid
        pkt._m2srwd_header.mem_opcode = m2srwd_header__mem_opcode
        pkt._m2srwd_header.meta_field = m2srwd_header__meta_field
        pkt._m2srwd_header.meta_value = m2srwd_header__meta_value
        pkt._m2srwd_header.snp_type = m2srwd_header__snp_type
        pkt._m2srwd_header.ld_id = m2srwd_header__ld_id
        pkt._m2srwd_header.addr = m2srwd_header__addr
        if data is not None:
            ptr = 17
            n = len(data)
            if 17 + n > 512:
                raise ValueError("data too large")
            pkt._buf[ptr:ptr+n] = data
            pkt._data_length = n
        pkt._system_header.payload_length = 17 + pkt._data_length
        return pkt

    def assign(self, system_header__payload_type, cxl_mem_header__msg_class, m2srwd_header__valid, m2srwd_header__mem_opcode, m2srwd_header__meta_field, m2srwd_header__meta_value, m2srwd_header__snp_type, m2srwd_header__ld_id, m2srwd_header__addr, data: bytes = None):
        self._buf = bytearray(512)
        self._system_header._attach(self._buf, 0)
        self._cxl_mem_header._attach(self._buf, 2)
        self._m2srwd_header._attach(self._buf, 4)
        self._system_header.payload_type = system_header__payload_type
        self._cxl_mem_header.msg_class = cxl_mem_header__msg_class
        self._m2srwd_header.valid = m2srwd_header__valid
        self._m2srwd_header.mem_opcode = m2srwd_header__mem_opcode
        self._m2srwd_header.meta_field = m2srwd_header__meta_field
        self._m2srwd_header.meta_value = m2srwd_header__meta_value
        self._m2srwd_header.snp_type = m2srwd_header__snp_type
        self._m2srwd_header.ld_id = m2srwd_header__ld_id
        self._m2srwd_header.addr = m2srwd_header__addr
        if data is not None:
            ptr = 17
            n = len(data)
            self._buf[ptr:ptr+n] = data
            self._data_length = n
        self._system_header.payload_length = 17 + self._data_length

    def set_data(self, data):
        n = len(data)
        if 17 + n > 512:
            raise ValueError("packet too large")
        self._buf[17:17+n] = data
        self._data_length = n

    def get_data(self):
        return bytes(self._buf[17:17+self._data_length])

    def get_payload_offset(self):
        return 17

    def get_size(self):
        return 17 + self._data_length

    def get_byte_offset(self, other):
        if isinstance(other, SystemHeader):
            return 0
        elif isinstance(other, CxlMemHeader):
            return 2
        elif isinstance(other, CxlMemM2SRwDHeader):
            return 4
        raise TypeError(f'unsupported header type: {type(other)}')

    def set_bytes(self, offset, data):
        n = len(data)
        self._buf[offset:offset+n] = data

    def write_bits(self, start_bit, width, value):
        _write_bits(self._buf, start_bit, width, value)

    def read_bits(self, start_bit, width):
        return _read_bits(self._buf, start_bit, width)

    def get_bytes(self, start, length):
        return bytes(self._buf[start:start+length])

    def __len__(self):
        return 17 + self._data_length

    def __bytes__(self):
        return bytes(self._buf[:17 + self._data_length])

    def __iter__(self):
        return iter(self._buf[:17 + self._data_length])

    def __getitem__(self, key):
        return self._buf[:17 + self._data_length][key]

    def __enter__(self):
        return self

    def __exit__(self, *a):
        pass

CxlMemM2SRwDPacket = _GenCxlMemM2SRwDPacket

class _GenCxlMemM2SBIRspPacket:
    _header_size = 9

    def __init__(self, payload=None):
        self._buf = bytearray(512)
        self._data_length = 0
        self._system_header = SystemHeader()
        self._system_header._attach(self._buf, 0)
        self._cxl_mem_header = CxlMemHeader()
        self._cxl_mem_header._attach(self._buf, 2)
        self._m2sbirsp_header = CxlMemM2SBIRspHeader()
        self._m2sbirsp_header._attach(self._buf, 4)
        if payload is not None:
            n = len(payload)
            if n > 512:
                raise ValueError("packet too large")
            self._buf[:n] = payload
            self._data_length = n - 9

    @property
    def system_header(self):
        return self._system_header

    @property
    def cxl_mem_header(self):
        return self._cxl_mem_header

    @property
    def m2sbirsp_header(self):
        return self._m2sbirsp_header

    @classmethod
    def create(cls, system_header__payload_type, cxl_mem_header__msg_class, m2sbirsp_header__valid, m2sbirsp_header__opcode, m2sbirsp_header__low_addr, m2sbirsp_header__bi_id, m2sbirsp_header__bi_tag, data: bytes = None):
        pkt = cls()
        pkt._buf = bytearray(512)
        pkt._system_header._attach(pkt._buf, 0)
        pkt._cxl_mem_header._attach(pkt._buf, 2)
        pkt._m2sbirsp_header._attach(pkt._buf, 4)
        pkt._system_header.payload_type = system_header__payload_type
        pkt._cxl_mem_header.msg_class = cxl_mem_header__msg_class
        pkt._m2sbirsp_header.valid = m2sbirsp_header__valid
        pkt._m2sbirsp_header.opcode = m2sbirsp_header__opcode
        pkt._m2sbirsp_header.low_addr = m2sbirsp_header__low_addr
        pkt._m2sbirsp_header.bi_id = m2sbirsp_header__bi_id
        pkt._m2sbirsp_header.bi_tag = m2sbirsp_header__bi_tag
        if data is not None:
            ptr = 9
            n = len(data)
            if 9 + n > 512:
                raise ValueError("data too large")
            pkt._buf[ptr:ptr+n] = data
            pkt._data_length = n
        pkt._system_header.payload_length = 9 + pkt._data_length
        return pkt

    def assign(self, system_header__payload_type, cxl_mem_header__msg_class, m2sbirsp_header__valid, m2sbirsp_header__opcode, m2sbirsp_header__low_addr, m2sbirsp_header__bi_id, m2sbirsp_header__bi_tag, data: bytes = None):
        self._buf = bytearray(512)
        self._system_header._attach(self._buf, 0)
        self._cxl_mem_header._attach(self._buf, 2)
        self._m2sbirsp_header._attach(self._buf, 4)
        self._system_header.payload_type = system_header__payload_type
        self._cxl_mem_header.msg_class = cxl_mem_header__msg_class
        self._m2sbirsp_header.valid = m2sbirsp_header__valid
        self._m2sbirsp_header.opcode = m2sbirsp_header__opcode
        self._m2sbirsp_header.low_addr = m2sbirsp_header__low_addr
        self._m2sbirsp_header.bi_id = m2sbirsp_header__bi_id
        self._m2sbirsp_header.bi_tag = m2sbirsp_header__bi_tag
        if data is not None:
            ptr = 9
            n = len(data)
            self._buf[ptr:ptr+n] = data
            self._data_length = n
        self._system_header.payload_length = 9 + self._data_length

    def set_data(self, data):
        n = len(data)
        if 9 + n > 512:
            raise ValueError("packet too large")
        self._buf[9:9+n] = data
        self._data_length = n

    def get_data(self):
        return bytes(self._buf[9:9+self._data_length])

    def get_payload_offset(self):
        return 9

    def get_size(self):
        return 9 + self._data_length

    def get_byte_offset(self, other):
        if isinstance(other, SystemHeader):
            return 0
        elif isinstance(other, CxlMemHeader):
            return 2
        elif isinstance(other, CxlMemM2SBIRspHeader):
            return 4
        raise TypeError(f'unsupported header type: {type(other)}')

    def set_bytes(self, offset, data):
        n = len(data)
        self._buf[offset:offset+n] = data

    def write_bits(self, start_bit, width, value):
        _write_bits(self._buf, start_bit, width, value)

    def read_bits(self, start_bit, width):
        return _read_bits(self._buf, start_bit, width)

    def get_bytes(self, start, length):
        return bytes(self._buf[start:start+length])

    def __len__(self):
        return 9 + self._data_length

    def __bytes__(self):
        return bytes(self._buf[:9 + self._data_length])

    def __iter__(self):
        return iter(self._buf[:9 + self._data_length])

    def __getitem__(self, key):
        return self._buf[:9 + self._data_length][key]

    def __enter__(self):
        return self

    def __exit__(self, *a):
        pass

CxlMemM2SBIRspPacket = _GenCxlMemM2SBIRspPacket

class _GenCxlMemS2MBISnpPacket:
    _header_size = 14

    def __init__(self, payload=None):
        self._buf = bytearray(512)
        self._data_length = 0
        self._system_header = SystemHeader()
        self._system_header._attach(self._buf, 0)
        self._cxl_mem_header = CxlMemHeader()
        self._cxl_mem_header._attach(self._buf, 2)
        self._s2mbisnp_header = CxlMemS2MBISnpHeader()
        self._s2mbisnp_header._attach(self._buf, 4)
        if payload is not None:
            n = len(payload)
            if n > 512:
                raise ValueError("packet too large")
            self._buf[:n] = payload
            self._data_length = n - 14

    @property
    def system_header(self):
        return self._system_header

    @property
    def cxl_mem_header(self):
        return self._cxl_mem_header

    @property
    def s2mbisnp_header(self):
        return self._s2mbisnp_header

    @classmethod
    def create(cls, system_header__payload_type, cxl_mem_header__msg_class, s2mbisnp_header__valid, s2mbisnp_header__opcode, s2mbisnp_header__bi_id, s2mbisnp_header__bi_tag, s2mbisnp_header__addr, data: bytes = None):
        pkt = cls()
        pkt._buf = bytearray(512)
        pkt._system_header._attach(pkt._buf, 0)
        pkt._cxl_mem_header._attach(pkt._buf, 2)
        pkt._s2mbisnp_header._attach(pkt._buf, 4)
        pkt._system_header.payload_type = system_header__payload_type
        pkt._cxl_mem_header.msg_class = cxl_mem_header__msg_class
        pkt._s2mbisnp_header.valid = s2mbisnp_header__valid
        pkt._s2mbisnp_header.opcode = s2mbisnp_header__opcode
        pkt._s2mbisnp_header.bi_id = s2mbisnp_header__bi_id
        pkt._s2mbisnp_header.bi_tag = s2mbisnp_header__bi_tag
        pkt._s2mbisnp_header.addr = s2mbisnp_header__addr
        if data is not None:
            ptr = 14
            n = len(data)
            if 14 + n > 512:
                raise ValueError("data too large")
            pkt._buf[ptr:ptr+n] = data
            pkt._data_length = n
        pkt._system_header.payload_length = 14 + pkt._data_length
        return pkt

    def assign(self, system_header__payload_type, cxl_mem_header__msg_class, s2mbisnp_header__valid, s2mbisnp_header__opcode, s2mbisnp_header__bi_id, s2mbisnp_header__bi_tag, s2mbisnp_header__addr, data: bytes = None):
        self._buf = bytearray(512)
        self._system_header._attach(self._buf, 0)
        self._cxl_mem_header._attach(self._buf, 2)
        self._s2mbisnp_header._attach(self._buf, 4)
        self._system_header.payload_type = system_header__payload_type
        self._cxl_mem_header.msg_class = cxl_mem_header__msg_class
        self._s2mbisnp_header.valid = s2mbisnp_header__valid
        self._s2mbisnp_header.opcode = s2mbisnp_header__opcode
        self._s2mbisnp_header.bi_id = s2mbisnp_header__bi_id
        self._s2mbisnp_header.bi_tag = s2mbisnp_header__bi_tag
        self._s2mbisnp_header.addr = s2mbisnp_header__addr
        if data is not None:
            ptr = 14
            n = len(data)
            self._buf[ptr:ptr+n] = data
            self._data_length = n
        self._system_header.payload_length = 14 + self._data_length

    def set_data(self, data):
        n = len(data)
        if 14 + n > 512:
            raise ValueError("packet too large")
        self._buf[14:14+n] = data
        self._data_length = n

    def get_data(self):
        return bytes(self._buf[14:14+self._data_length])

    def get_payload_offset(self):
        return 14

    def get_size(self):
        return 14 + self._data_length

    def get_byte_offset(self, other):
        if isinstance(other, SystemHeader):
            return 0
        elif isinstance(other, CxlMemHeader):
            return 2
        elif isinstance(other, CxlMemS2MBISnpHeader):
            return 4
        raise TypeError(f'unsupported header type: {type(other)}')

    def set_bytes(self, offset, data):
        n = len(data)
        self._buf[offset:offset+n] = data

    def write_bits(self, start_bit, width, value):
        _write_bits(self._buf, start_bit, width, value)

    def read_bits(self, start_bit, width):
        return _read_bits(self._buf, start_bit, width)

    def get_bytes(self, start, length):
        return bytes(self._buf[start:start+length])

    def __len__(self):
        return 14 + self._data_length

    def __bytes__(self):
        return bytes(self._buf[:14 + self._data_length])

    def __iter__(self):
        return iter(self._buf[:14 + self._data_length])

    def __getitem__(self, key):
        return self._buf[:14 + self._data_length][key]

    def __enter__(self):
        return self

    def __exit__(self, *a):
        pass

CxlMemS2MBISnpPacket = _GenCxlMemS2MBISnpPacket

class _GenCxlMemS2MNDRPacket:
    _header_size = 9

    def __init__(self, payload=None):
        self._buf = bytearray(512)
        self._data_length = 0
        self._system_header = SystemHeader()
        self._system_header._attach(self._buf, 0)
        self._cxl_mem_header = CxlMemHeader()
        self._cxl_mem_header._attach(self._buf, 2)
        self._s2mndr_header = CxlMemS2MNDRHeader()
        self._s2mndr_header._attach(self._buf, 4)
        if payload is not None:
            n = len(payload)
            if n > 512:
                raise ValueError("packet too large")
            self._buf[:n] = payload
            self._data_length = n - 9

    @property
    def system_header(self):
        return self._system_header

    @property
    def cxl_mem_header(self):
        return self._cxl_mem_header

    @property
    def s2mndr_header(self):
        return self._s2mndr_header

    @classmethod
    def create(cls, system_header__payload_type, cxl_mem_header__msg_class, s2mndr_header__valid, s2mndr_header__opcode, s2mndr_header__meta_field, s2mndr_header__meta_value, s2mndr_header__ld_id, data: bytes = None):
        pkt = cls()
        pkt._buf = bytearray(512)
        pkt._system_header._attach(pkt._buf, 0)
        pkt._cxl_mem_header._attach(pkt._buf, 2)
        pkt._s2mndr_header._attach(pkt._buf, 4)
        pkt._system_header.payload_type = system_header__payload_type
        pkt._cxl_mem_header.msg_class = cxl_mem_header__msg_class
        pkt._s2mndr_header.valid = s2mndr_header__valid
        pkt._s2mndr_header.opcode = s2mndr_header__opcode
        pkt._s2mndr_header.meta_field = s2mndr_header__meta_field
        pkt._s2mndr_header.meta_value = s2mndr_header__meta_value
        pkt._s2mndr_header.ld_id = s2mndr_header__ld_id
        if data is not None:
            ptr = 9
            n = len(data)
            if 9 + n > 512:
                raise ValueError("data too large")
            pkt._buf[ptr:ptr+n] = data
            pkt._data_length = n
        pkt._system_header.payload_length = 9 + pkt._data_length
        return pkt

    def assign(self, system_header__payload_type, cxl_mem_header__msg_class, s2mndr_header__valid, s2mndr_header__opcode, s2mndr_header__meta_field, s2mndr_header__meta_value, s2mndr_header__ld_id, data: bytes = None):
        self._buf = bytearray(512)
        self._system_header._attach(self._buf, 0)
        self._cxl_mem_header._attach(self._buf, 2)
        self._s2mndr_header._attach(self._buf, 4)
        self._system_header.payload_type = system_header__payload_type
        self._cxl_mem_header.msg_class = cxl_mem_header__msg_class
        self._s2mndr_header.valid = s2mndr_header__valid
        self._s2mndr_header.opcode = s2mndr_header__opcode
        self._s2mndr_header.meta_field = s2mndr_header__meta_field
        self._s2mndr_header.meta_value = s2mndr_header__meta_value
        self._s2mndr_header.ld_id = s2mndr_header__ld_id
        if data is not None:
            ptr = 9
            n = len(data)
            self._buf[ptr:ptr+n] = data
            self._data_length = n
        self._system_header.payload_length = 9 + self._data_length

    def set_data(self, data):
        n = len(data)
        if 9 + n > 512:
            raise ValueError("packet too large")
        self._buf[9:9+n] = data
        self._data_length = n

    def get_data(self):
        return bytes(self._buf[9:9+self._data_length])

    def get_payload_offset(self):
        return 9

    def get_size(self):
        return 9 + self._data_length

    def get_byte_offset(self, other):
        if isinstance(other, SystemHeader):
            return 0
        elif isinstance(other, CxlMemHeader):
            return 2
        elif isinstance(other, CxlMemS2MNDRHeader):
            return 4
        raise TypeError(f'unsupported header type: {type(other)}')

    def set_bytes(self, offset, data):
        n = len(data)
        self._buf[offset:offset+n] = data

    def write_bits(self, start_bit, width, value):
        _write_bits(self._buf, start_bit, width, value)

    def read_bits(self, start_bit, width):
        return _read_bits(self._buf, start_bit, width)

    def get_bytes(self, start, length):
        return bytes(self._buf[start:start+length])

    def __len__(self):
        return 9 + self._data_length

    def __bytes__(self):
        return bytes(self._buf[:9 + self._data_length])

    def __iter__(self):
        return iter(self._buf[:9 + self._data_length])

    def __getitem__(self, key):
        return self._buf[:9 + self._data_length][key]

    def __enter__(self):
        return self

    def __exit__(self, *a):
        pass

CxlMemS2MNDRPacket = _GenCxlMemS2MNDRPacket

class _GenCxlMemS2MDRSPacket:
    _header_size = 9

    def __init__(self, payload=None):
        self._buf = bytearray(512)
        self._data_length = 0
        self._system_header = SystemHeader()
        self._system_header._attach(self._buf, 0)
        self._cxl_mem_header = CxlMemHeader()
        self._cxl_mem_header._attach(self._buf, 2)
        self._s2mdrs_header = CxlMemS2MDRSHeader()
        self._s2mdrs_header._attach(self._buf, 4)
        if payload is not None:
            n = len(payload)
            if n > 512:
                raise ValueError("packet too large")
            self._buf[:n] = payload
            self._data_length = n - 9

    @property
    def system_header(self):
        return self._system_header

    @property
    def cxl_mem_header(self):
        return self._cxl_mem_header

    @property
    def s2mdrs_header(self):
        return self._s2mdrs_header

    @classmethod
    def create(cls, system_header__payload_type, cxl_mem_header__msg_class, s2mdrs_header__valid, s2mdrs_header__opcode, s2mdrs_header__meta_field, s2mdrs_header__meta_value, s2mdrs_header__ld_id, data: bytes = None):
        pkt = cls()
        pkt._buf = bytearray(512)
        pkt._system_header._attach(pkt._buf, 0)
        pkt._cxl_mem_header._attach(pkt._buf, 2)
        pkt._s2mdrs_header._attach(pkt._buf, 4)
        pkt._system_header.payload_type = system_header__payload_type
        pkt._cxl_mem_header.msg_class = cxl_mem_header__msg_class
        pkt._s2mdrs_header.valid = s2mdrs_header__valid
        pkt._s2mdrs_header.opcode = s2mdrs_header__opcode
        pkt._s2mdrs_header.meta_field = s2mdrs_header__meta_field
        pkt._s2mdrs_header.meta_value = s2mdrs_header__meta_value
        pkt._s2mdrs_header.ld_id = s2mdrs_header__ld_id
        if data is not None:
            ptr = 9
            n = len(data)
            if 9 + n > 512:
                raise ValueError("data too large")
            pkt._buf[ptr:ptr+n] = data
            pkt._data_length = n
        pkt._system_header.payload_length = 9 + pkt._data_length
        return pkt

    def assign(self, system_header__payload_type, cxl_mem_header__msg_class, s2mdrs_header__valid, s2mdrs_header__opcode, s2mdrs_header__meta_field, s2mdrs_header__meta_value, s2mdrs_header__ld_id, data: bytes = None):
        self._buf = bytearray(512)
        self._system_header._attach(self._buf, 0)
        self._cxl_mem_header._attach(self._buf, 2)
        self._s2mdrs_header._attach(self._buf, 4)
        self._system_header.payload_type = system_header__payload_type
        self._cxl_mem_header.msg_class = cxl_mem_header__msg_class
        self._s2mdrs_header.valid = s2mdrs_header__valid
        self._s2mdrs_header.opcode = s2mdrs_header__opcode
        self._s2mdrs_header.meta_field = s2mdrs_header__meta_field
        self._s2mdrs_header.meta_value = s2mdrs_header__meta_value
        self._s2mdrs_header.ld_id = s2mdrs_header__ld_id
        if data is not None:
            ptr = 9
            n = len(data)
            self._buf[ptr:ptr+n] = data
            self._data_length = n
        self._system_header.payload_length = 9 + self._data_length

    def set_data(self, data):
        n = len(data)
        if 9 + n > 512:
            raise ValueError("packet too large")
        self._buf[9:9+n] = data
        self._data_length = n

    def get_data(self):
        return bytes(self._buf[9:9+self._data_length])

    def get_payload_offset(self):
        return 9

    def get_size(self):
        return 9 + self._data_length

    def get_byte_offset(self, other):
        if isinstance(other, SystemHeader):
            return 0
        elif isinstance(other, CxlMemHeader):
            return 2
        elif isinstance(other, CxlMemS2MDRSHeader):
            return 4
        raise TypeError(f'unsupported header type: {type(other)}')

    def set_bytes(self, offset, data):
        n = len(data)
        self._buf[offset:offset+n] = data

    def write_bits(self, start_bit, width, value):
        _write_bits(self._buf, start_bit, width, value)

    def read_bits(self, start_bit, width):
        return _read_bits(self._buf, start_bit, width)

    def get_bytes(self, start, length):
        return bytes(self._buf[start:start+length])

    def __len__(self):
        return 9 + self._data_length

    def __bytes__(self):
        return bytes(self._buf[:9 + self._data_length])

    def __iter__(self):
        return iter(self._buf[:9 + self._data_length])

    def __getitem__(self, key):
        return self._buf[:9 + self._data_length][key]

    def __enter__(self):
        return self

    def __exit__(self, *a):
        pass

CxlMemS2MDRSPacket = _GenCxlMemS2MDRSPacket

class _GenCciBasePacket:
    _header_size = 4

    def __init__(self, payload=None):
        self._buf = bytearray(512)
        self._data_length = 0
        self._system_header = SystemHeader()
        self._system_header._attach(self._buf, 0)
        self._cci_header = CciHeader()
        self._cci_header._attach(self._buf, 2)
        if payload is not None:
            n = len(payload)
            if n > 512:
                raise ValueError("packet too large")
            self._buf[:n] = payload
            self._data_length = n - 4

    @property
    def system_header(self):
        return self._system_header

    @property
    def cci_header(self):
        return self._cci_header

    def set_data(self, data):
        n = len(data)
        if 4 + n > 512:
            raise ValueError("packet too large")
        self._buf[4:4+n] = data
        self._data_length = n

    def get_data(self):
        return bytes(self._buf[4:4+self._data_length])

    def get_payload_offset(self):
        return 4

    def get_size(self):
        return 4 + self._data_length

    def get_byte_offset(self, other):
        if isinstance(other, SystemHeader):
            return 0
        elif isinstance(other, CciHeader):
            return 2
        raise TypeError(f'unsupported header type: {type(other)}')

    def set_bytes(self, offset, data):
        n = len(data)
        self._buf[offset:offset+n] = data

    def write_bits(self, start_bit, width, value):
        _write_bits(self._buf, start_bit, width, value)

    def read_bits(self, start_bit, width):
        return _read_bits(self._buf, start_bit, width)

    def get_bytes(self, start, length):
        return bytes(self._buf[start:start+length])

    def __len__(self):
        return 4 + self._data_length

    def __bytes__(self):
        return bytes(self._buf[:4 + self._data_length])

    def __iter__(self):
        return iter(self._buf[:4 + self._data_length])

    def __getitem__(self, key):
        return self._buf[:4 + self._data_length][key]

    def __enter__(self):
        return self

    def __exit__(self, *a):
        pass

CciBasePacket = _GenCciBasePacket

class _GenCciMessagePacket:
    _header_size = 12

    def __init__(self, payload=None):
        self._buf = bytearray(512)
        self._data_length = 0
        self._cci_msg_header = CciMessageHeader()
        self._cci_msg_header._attach(self._buf, 0)
        if payload is not None:
            n = len(payload)
            if n > 512:
                raise ValueError("packet too large")
            self._buf[:n] = payload
            self._data_length = n - 12

    @property
    def cci_msg_header(self):
        return self._cci_msg_header

    def set_data(self, data):
        n = len(data)
        if 12 + n > 512:
            raise ValueError("packet too large")
        self._buf[12:12+n] = data
        self._data_length = n

    def get_data(self):
        return bytes(self._buf[12:12+self._data_length])

    def get_payload_offset(self):
        return 12

    def get_size(self):
        return 12 + self._data_length

    def get_byte_offset(self, other):
        if isinstance(other, CciMessageHeader):
            return 0
        raise TypeError(f'unsupported header type: {type(other)}')

    def set_bytes(self, offset, data):
        n = len(data)
        self._buf[offset:offset+n] = data

    def write_bits(self, start_bit, width, value):
        _write_bits(self._buf, start_bit, width, value)

    def read_bits(self, start_bit, width):
        return _read_bits(self._buf, start_bit, width)

    def get_bytes(self, start, length):
        return bytes(self._buf[start:start+length])

    def __len__(self):
        return 12 + self._data_length

    def __bytes__(self):
        return bytes(self._buf[:12 + self._data_length])

    def __iter__(self):
        return iter(self._buf[:12 + self._data_length])

    def __getitem__(self, key):
        return self._buf[:12 + self._data_length][key]

    def __enter__(self):
        return self

    def __exit__(self, *a):
        pass

CciMessagePacket = _GenCciMessagePacket

class _GenCciPayloadPacket:
    _header_size = 4

    def __init__(self, payload=None):
        self._buf = bytearray(512)
        self._data_length = 0
        self._system_header = SystemHeader()
        self._system_header._attach(self._buf, 0)
        self._cci_header = CciHeader()
        self._cci_header._attach(self._buf, 2)
        if payload is not None:
            n = len(payload)
            if n > 512:
                raise ValueError("packet too large")
            self._buf[:n] = payload
            self._data_length = n - 4

    @property
    def system_header(self):
        return self._system_header

    @property
    def cci_header(self):
        return self._cci_header

    def set_data(self, data):
        n = len(data)
        if 4 + n > 512:
            raise ValueError("packet too large")
        self._buf[4:4+n] = data
        self._data_length = n

    def get_data(self):
        return bytes(self._buf[4:4+self._data_length])

    def get_payload_offset(self):
        return 4

    def get_size(self):
        return 4 + self._data_length

    def get_byte_offset(self, other):
        if isinstance(other, SystemHeader):
            return 0
        elif isinstance(other, CciHeader):
            return 2
        raise TypeError(f'unsupported header type: {type(other)}')

    def set_bytes(self, offset, data):
        n = len(data)
        self._buf[offset:offset+n] = data

    def write_bits(self, start_bit, width, value):
        _write_bits(self._buf, start_bit, width, value)

    def read_bits(self, start_bit, width):
        return _read_bits(self._buf, start_bit, width)

    def get_bytes(self, start, length):
        return bytes(self._buf[start:start+length])

    def __len__(self):
        return 4 + self._data_length

    def __bytes__(self):
        return bytes(self._buf[:4 + self._data_length])

    def __iter__(self):
        return iter(self._buf[:4 + self._data_length])

    def __getitem__(self, key):
        return self._buf[:4 + self._data_length][key]

    def __enter__(self):
        return self

    def __exit__(self, *a):
        pass

CciPayloadPacket = _GenCciPayloadPacket

class _GenCciRequestPacket:
    _header_size = 16

    def __init__(self, payload=None):
        self._buf = bytearray(512)
        self._data_length = 0
        self._system_header = SystemHeader()
        self._system_header._attach(self._buf, 0)
        self._cci_header = CciHeader()
        self._cci_header._attach(self._buf, 2)
        self._cci_msg_header = CciMessageHeader()
        self._cci_msg_header._attach(self._buf, 4)
        if payload is not None:
            n = len(payload)
            if n > 512:
                raise ValueError("packet too large")
            self._buf[:n] = payload
            self._data_length = n - 16

    @property
    def system_header(self):
        return self._system_header

    @property
    def cci_header(self):
        return self._cci_header

    @property
    def cci_msg_header(self):
        return self._cci_msg_header

    def set_data(self, data):
        n = len(data)
        if 16 + n > 512:
            raise ValueError("packet too large")
        self._buf[16:16+n] = data
        self._data_length = n

    def get_data(self):
        return bytes(self._buf[16:16+self._data_length])

    def get_payload_offset(self):
        return 16

    def get_size(self):
        return 16 + self._data_length

    def get_byte_offset(self, other):
        if isinstance(other, SystemHeader):
            return 0
        elif isinstance(other, CciHeader):
            return 2
        elif isinstance(other, CciMessageHeader):
            return 4
        raise TypeError(f'unsupported header type: {type(other)}')

    def set_bytes(self, offset, data):
        n = len(data)
        self._buf[offset:offset+n] = data

    def write_bits(self, start_bit, width, value):
        _write_bits(self._buf, start_bit, width, value)

    def read_bits(self, start_bit, width):
        return _read_bits(self._buf, start_bit, width)

    def get_bytes(self, start, length):
        return bytes(self._buf[start:start+length])

    def __len__(self):
        return 16 + self._data_length

    def __bytes__(self):
        return bytes(self._buf[:16 + self._data_length])

    def __iter__(self):
        return iter(self._buf[:16 + self._data_length])

    def __getitem__(self, key):
        return self._buf[:16 + self._data_length][key]

    def __enter__(self):
        return self

    def __exit__(self, *a):
        pass

CciRequestPacket = _GenCciRequestPacket

class _GenCciResponsePacket:
    _header_size = 16

    def __init__(self, payload=None):
        self._buf = bytearray(512)
        self._data_length = 0
        self._system_header = SystemHeader()
        self._system_header._attach(self._buf, 0)
        self._cci_header = CciHeader()
        self._cci_header._attach(self._buf, 2)
        self._cci_msg_header = CciMessageHeader()
        self._cci_msg_header._attach(self._buf, 4)
        if payload is not None:
            n = len(payload)
            if n > 512:
                raise ValueError("packet too large")
            self._buf[:n] = payload
            self._data_length = n - 16

    @property
    def system_header(self):
        return self._system_header

    @property
    def cci_header(self):
        return self._cci_header

    @property
    def cci_msg_header(self):
        return self._cci_msg_header

    def set_data(self, data):
        n = len(data)
        if 16 + n > 512:
            raise ValueError("packet too large")
        self._buf[16:16+n] = data
        self._data_length = n

    def get_data(self):
        return bytes(self._buf[16:16+self._data_length])

    def get_payload_offset(self):
        return 16

    def get_size(self):
        return 16 + self._data_length

    def get_byte_offset(self, other):
        if isinstance(other, SystemHeader):
            return 0
        elif isinstance(other, CciHeader):
            return 2
        elif isinstance(other, CciMessageHeader):
            return 4
        raise TypeError(f'unsupported header type: {type(other)}')

    def set_bytes(self, offset, data):
        n = len(data)
        self._buf[offset:offset+n] = data

    def write_bits(self, start_bit, width, value):
        _write_bits(self._buf, start_bit, width, value)

    def read_bits(self, start_bit, width):
        return _read_bits(self._buf, start_bit, width)

    def get_bytes(self, start, length):
        return bytes(self._buf[start:start+length])

    def __len__(self):
        return 16 + self._data_length

    def __bytes__(self):
        return bytes(self._buf[:16 + self._data_length])

    def __iter__(self):
        return iter(self._buf[:16 + self._data_length])

    def __getitem__(self, key):
        return self._buf[:16 + self._data_length][key]

    def __enter__(self):
        return self

    def __exit__(self, *a):
        pass

CciResponsePacket = _GenCciResponsePacket

class _GenPbrBasePacket:
    _header_size = 6

    def __init__(self, payload=None):
        self._buf = bytearray(512)
        self._data_length = 0
        self._system_header = SystemHeader()
        self._system_header._attach(self._buf, 0)
        self._pbr_header = PbrHeader()
        self._pbr_header._attach(self._buf, 2)
        if payload is not None:
            n = len(payload)
            if n > 512:
                raise ValueError("packet too large")
            self._buf[:n] = payload
            self._data_length = n - 6

    @property
    def system_header(self):
        return self._system_header

    @property
    def pbr_header(self):
        return self._pbr_header

    @classmethod
    def create(cls, system_header__payload_type, pbr_header__spid, pbr_header__dpid, data: bytes = None):
        pkt = cls()
        pkt._buf = bytearray(512)
        pkt._system_header._attach(pkt._buf, 0)
        pkt._pbr_header._attach(pkt._buf, 2)
        pkt._system_header.payload_type = system_header__payload_type
        pkt._pbr_header.spid = pbr_header__spid
        pkt._pbr_header.dpid = pbr_header__dpid
        if data is not None:
            ptr = 6
            n = len(data)
            if 6 + n > 512:
                raise ValueError("data too large")
            pkt._buf[ptr:ptr+n] = data
            pkt._data_length = n
        pkt._system_header.payload_length = 6 + pkt._data_length
        return pkt

    def assign(self, system_header__payload_type, pbr_header__spid, pbr_header__dpid, data: bytes = None):
        self._buf = bytearray(512)
        self._system_header._attach(self._buf, 0)
        self._pbr_header._attach(self._buf, 2)
        self._system_header.payload_type = system_header__payload_type
        self._pbr_header.spid = pbr_header__spid
        self._pbr_header.dpid = pbr_header__dpid
        if data is not None:
            ptr = 6
            n = len(data)
            self._buf[ptr:ptr+n] = data
            self._data_length = n
        self._system_header.payload_length = 6 + self._data_length

    def set_data(self, data):
        n = len(data)
        if 6 + n > 512:
            raise ValueError("packet too large")
        self._buf[6:6+n] = data
        self._data_length = n

    def get_data(self):
        return bytes(self._buf[6:6+self._data_length])

    def get_payload_offset(self):
        return 6

    def get_size(self):
        return 6 + self._data_length

    def get_byte_offset(self, other):
        if isinstance(other, SystemHeader):
            return 0
        elif isinstance(other, PbrHeader):
            return 2
        raise TypeError(f'unsupported header type: {type(other)}')

    def set_bytes(self, offset, data):
        n = len(data)
        self._buf[offset:offset+n] = data

    def write_bits(self, start_bit, width, value):
        _write_bits(self._buf, start_bit, width, value)

    def read_bits(self, start_bit, width):
        return _read_bits(self._buf, start_bit, width)

    def get_bytes(self, start, length):
        return bytes(self._buf[start:start+length])

    def __len__(self):
        return 6 + self._data_length

    def __bytes__(self):
        return bytes(self._buf[:6 + self._data_length])

    def __iter__(self):
        return iter(self._buf[:6 + self._data_length])

    def __getitem__(self, key):
        return self._buf[:6 + self._data_length][key]

    def __enter__(self):
        return self

    def __exit__(self, *a):
        pass

PbrBasePacket = _GenPbrBasePacket

# ---------------------------------------------------------------------------
# Override with compiled Cython extension if available
# ---------------------------------------------------------------------------
try:
    from opencis.cxl.transport._packet_structs_cython import *
except ImportError:
    pass
